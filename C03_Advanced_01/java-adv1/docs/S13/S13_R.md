# 스레드 풀과 Executor 프레임워크 1

> **표기 규칙**
> - 🔴 **정정** — 원문에 사실과 다르거나 오해를 부르는 내용
> - 🟡 **보강** — 틀리진 않지만 엄밀하지 않아 조건을 붙인 부분
> - 💡 **추가** — 지금 단계에서 함께 알아두면 좋은 내용

---

## 목차

1. [스레드를 직접 쓸 때의 문제](#1-스레드를-직접-쓸-때의-문제)
2. [스레드 풀과 Executor 프레임워크](#2-스레드-풀과-executor-프레임워크)
3. [ThreadPoolExecutor 기본 사용](#3-threadpoolexecutor-기본-사용)
4. [⭐ 스레드 생성 규칙 💡](#4--스레드-생성-규칙-)
5. [Callable과 Future](#5-callable과-future)
6. [Future의 동작 원리](#6-future의-동작-원리)
7. [⭐ 왜 submit과 get을 분리했나](#7--왜-submit과-get을-분리했나)
8. [Future API와 취소](#8-future-api와-취소)
9. [예외 처리](#9-예외-처리)
10. [invokeAll과 invokeAny](#10-invokeall과-invokeany)
11. [⚠️ 스레드 풀은 반드시 닫아야 한다 💡](#11-️-스레드-풀은-반드시-닫아야-한다-)
12. [원문 점검 결과 요약](#12-원문-점검-결과-요약)
13. [최종 정리](#13-최종-정리)

---

## 1. 스레드를 직접 쓸 때의 문제

| 문제 | 내용 |
| --- | --- |
| **생성 비용** | 스택 메모리 할당 + 커널 시스템 콜. 작업보다 생성이 더 오래 걸릴 수 있다 |
| **관리 문제** | 요청마다 스레드를 만들면, 요청 1만 개 = 스레드 1만 개 → 시스템이 버티지 못한다 |
| **Runnable의 불편함** | 반환값이 없고, 체크 예외를 던질 수 없다 |

🟡 **"스레드 하나는 보통 1MB 이상의 메모리를 사용"을 정확히 하면:**
64비트 JVM의 기본 스택 크기(`-Xss`)가 약 **1MB이고, 이건 "예약"되는 가상 메모리 크기**다. 실제 물리 메모리는 스택을 깊게 쓸수록 늘어난다. 그래도 스레드 수천 개면 예약 공간만으로도 부담이 크고, 커널 자원(스레드 디스크립터, 스케줄러 부담)은 별도다.

> 첫 글에서 정리한 **"`Thread` 객체 생성은 싸고, 비싼 건 `start()`의 커널 호출과 스택 할당"**이 이 문제의 정체다.

---

## 2. 스레드 풀과 Executor 프레임워크

### 2.1 스레드 풀의 아이디어

```text
① 스레드를 미리 만들어 풀에 넣어둔다
② 작업이 오면 풀에서 놀고 있는 스레드 하나를 꺼내 처리한다
③ 끝나면 종료하지 않고 풀로 돌려보낸다 → 재사용
④ 작업이 없으면 WAITING 으로 대기한다 (CPU 안 씀)
```

| 문제 | 스레드 풀의 해결 |
| --- | --- |
| 생성 비용 | 한 번 만들고 **재사용** |
| 관리 문제 | 풀 크기로 **최대 스레드 수를 제한** |

### 2.2 💡 "대기 상태로 관리한다"의 정체

원문의 "작업이 없으면 대기(Waiting) 상태로 관리하고, 작업 요청이 오면 Runnable 상태로 변경"은 **앞 두 글에서 만든 그것**이다.

```text
   작업을 넣는 쪽 (생산자)          풀의 스레드들 (소비자)

   es.execute(task) ──▶  BlockingQueue  ──▶ take()
                                              │
                          비어 있으면 ⭐ WAITING 으로 잠듦
                          (Condition.await → LockSupport.park)
                                              │
                          작업이 들어오면 signal 로 깨어남
```

> ⭐ **스레드 풀은 생산자-소비자 문제의 완성품이다.**
> 앞 글의 `BlockingQueue`가 작업 큐로 쓰이고, 놀고 있는 스레드는 `take()`에서 `WAITING`으로 잠들어 있다. yield로 양보하는 게 아니라 **잘 자고 있는 것**이다.

### 2.3 Executor 인터페이스

```java
public interface Executor {
    void execute(Runnable command);
}
```

```java
public interface ExecutorService extends Executor, AutoCloseable {
    <T> Future<T> submit(Callable<T> task);
    void shutdown();
    List<Runnable> shutdownNow();
    boolean awaitTermination(long timeout, TimeUnit unit) throws InterruptedException;
    <T> List<Future<T>> invokeAll(Collection<? extends Callable<T>> tasks) throws InterruptedException;
    <T> T invokeAny(Collection<? extends Callable<T>> tasks) throws InterruptedException, ExecutionException;

    @Override
    default void close() { ... }
    // ...
}
```

| | 역할 |
| --- | --- |
| `Executor` | 작업 실행만 (`execute`) |
| `ExecutorService` | + 작업 제출(`submit`), 종료, 일괄 처리. **대부분 이걸 쓴다** |
| `ThreadPoolExecutor` | 대표 구현체 |

🟡 **`AutoCloseable`과 `close()`는 Java 19부터다.** 그 이전 버전에서는 `close()`가 없어 원문 예제의 `es.close()`가 컴파일되지 않는다. 11장에서 대안을 정리한다.

---

## 3. ThreadPoolExecutor 기본 사용

### 3.1 상태 출력 유틸

```java
public abstract class ExecutorUtils {

    public static void printState(ExecutorService executorService) {
        if (executorService instanceof ThreadPoolExecutor poolExecutor) {
            int pool = poolExecutor.getPoolSize();
            int active = poolExecutor.getActiveCount();
            int queuedTasks = poolExecutor.getQueue().size();
            long completedTask = poolExecutor.getCompletedTaskCount();
            log("[pool=" + pool + ", active=" + active + ", queuedTasks=" + queuedTasks +
                    ", completedTask=" + completedTask + "]");
        } else {
            log(executorService);
        }
    }
}
```

| 메서드 | 의미 |
| --- | --- |
| `getPoolSize()` | 풀이 관리하는 스레드 수 |
| `getActiveCount()` | 지금 작업 중인 스레드 수 |
| `getQueue().size()` | 큐에서 대기 중인 작업 수 |
| `getCompletedTaskCount()` | 완료된 작업 수 |

🟡 **`getActiveCount()`와 `getCompletedTaskCount()`는 근사값이다.** 공식 문서에 "approximate"라고 명시되어 있다. 조회하는 순간에도 스레드들이 움직이므로 정확한 스냅샷이 아니다. 앞 글의 `getState()`처럼 **모니터링용이지 흐름 제어용이 아니다.**

💡 `instanceof ThreadPoolExecutor poolExecutor`는 **패턴 매칭**(Java 16+)이다. 검사와 캐스팅을 한 번에 한다.

### 3.2 작업과 실행

```java
public class RunnableTask implements Runnable {

    private final String name;
    private int sleepMs = 1000;

    public RunnableTask(String name) { this.name = name; }
    public RunnableTask(String name, int sleepMs) { this.name = name; this.sleepMs = sleepMs; }

    @Override
    public void run() {
        log(name + " 시작");
        sleep(sleepMs);
        log(name + " 완료");
    }
}
```

```java
ExecutorService es = new ThreadPoolExecutor(
        2,                          // corePoolSize
        2,                          // maximumPoolSize
        0, TimeUnit.MILLISECONDS,   // keepAliveTime
        new LinkedBlockingQueue<>() // workQueue
);

printState(es);
es.execute(new RunnableTask("taskA"));   // 스레드 1 생성
es.execute(new RunnableTask("taskB"));   // 스레드 2 생성
es.execute(new RunnableTask("taskC"));   // 🟡 큐로 들어간다
es.execute(new RunnableTask("taskD"));
es.execute(new RunnableTask("taskE"));
printState(es);

sleep(5000);
printState(es);

es.close();
printState(es);
```

🟡 **원문 주석 "여기서부터는 쓰레드를 만들지 않고, 재사용"을 정확히 하면:** taskC는 **큐에 들어간다.** "재사용"은 그 뒤에 일어난다 — thread-1이나 thread-2가 자기 작업을 끝내고 **큐에서 taskC를 꺼낼 때**다.

### 3.3 실행 결과 읽기

```text
[     main] [pool=0, active=0, queuedTasks=0, completedTask=0]   ← 처음엔 0개로 시작
[pool-1-thread-1] taskA 시작
[     main] [pool=2, active=2, queuedTasks=3, completedTask=0]   ← 스레드 2개, 큐 3개
[pool-1-thread-2] taskB 시작
[pool-1-thread-2] taskB 완료
[pool-1-thread-1] taskA 완료
[pool-1-thread-2] taskC 시작      ⭐ thread-2 가 재사용됨
[pool-1-thread-1] taskD 시작      ⭐ thread-1 이 재사용됨
[pool-1-thread-1] taskD 완료
[pool-1-thread-1] taskE 시작
[pool-1-thread-2] taskC 완료
[pool-1-thread-1] taskE 완료
[     main] [pool=2, active=0, queuedTasks=0, completedTask=5]   ← 스레드는 남아서 대기
[     main] == shutdown ==
[     main] [pool=0, active=0, queuedTasks=0, completedTask=5]   ← close 후 0
```

**작업은 5개인데 스레드 이름은 `thread-1`, `thread-2` 두 개뿐이다.** 이게 재사용의 증거다.

```text
                     main (생산자)
                        │ execute(taskA..E)
                        ▼
 ┌──────────────── ThreadPoolExecutor ────────────────┐
 │                                                    │
 │   BlockingQueue   [taskC] [taskD] [taskE]          │
 │                       │                            │
 │              take()   ▼        take()              │
 │   ┌──────────────────┐   ┌──────────────────┐      │
 │   │ pool-1-thread-1  │   │ pool-1-thread-2  │ 소비자│
 │   │   taskA 실행     │   │   taskB 실행     │      │
 │   └──────────────────┘   └──────────────────┘      │
 └────────────────────────────────────────────────────┘
```

원문이 정확히 짚었다. **생산자는 `es.execute()`를 호출하는 스레드, 소비자는 풀의 스레드**다.

🔴 **오타**: "생산자: **ex**.execute(작업)" → **`es`**.execute(작업)

💡 **`pool-1-thread-1`이라는 이름**은 기본 `ThreadFactory`가 붙인다(`pool-풀번호-thread-스레드번호`). 첫 글에서 "실무에서는 스레드 이름을 반드시 지어준다"고 했는데, 스레드 풀에서는 **`ThreadFactory`를 직접 넘겨** `order-worker-1` 같은 이름을 붙인다.

### 3.4 생성자 매개변수

| 매개변수 | 의미 |
| --- | --- |
| `corePoolSize` | 기본으로 유지하는 스레드 수. **0개로 시작**해서 작업이 올 때 만든다 |
| `maximumPoolSize` | 최대 스레드 수 |
| `keepAliveTime`, `unit` | core를 **초과**해서 만든 스레드가, 일이 없을 때 살아있는 시간 |
| `workQueue` | 작업을 보관할 `BlockingQueue` |

🔴 **오타**: "작업이 들어올 때**가**지는" → **때까지는**, "이 시간**이** 동안" → **이 시간 동안**

---

## 4. ⭐ 스레드 생성 규칙 💡

생성자 매개변수를 소개했으니 **어떤 순서로 쓰이는지**를 정확히 알아야 한다. 실무에서 가장 많이 오해하는 부분이다.

### 4.1 규칙

```text
execute(task) 호출
   │
   ├─ ① 스레드 수 < corePoolSize ?
   │        → 새 스레드 생성  (⚠️ 놀고 있는 스레드가 있어도 새로 만든다)
   │
   ├─ ② 큐에 넣을 수 있나?
   │        → 큐에 넣는다
   │
   ├─ ③ 스레드 수 < maximumPoolSize ?
   │        → 초과 스레드 생성
   │
   └─ ④ 전부 불가
            → 거절 (RejectedExecutionException)
```

> ⭐ **핵심: 큐가 가득 차기 전에는 core를 넘는 스레드가 생기지 않는다.**
> "부하가 오면 max까지 늘어나겠지"가 아니라, **"큐가 넘쳐야 늘어난다"**다.

### 4.2 ⚠️ 무한 큐를 쓰면 maximumPoolSize는 장식이다

```java
new ThreadPoolExecutor(
    10, 100,                        // core 10, max 100 으로 설정했지만
    60, TimeUnit.SECONDS,
    new LinkedBlockingQueue<>()     // ⚠️ 크기 = Integer.MAX_VALUE
);
// → 큐가 절대 가득 차지 않는다
// → ③ 단계에 도달하지 못한다
// → 스레드는 영원히 10개. max=100 은 의미가 없다
```

앞 글에서 **"`LinkedBlockingQueue`의 기본 크기는 무한이다"**라고 경고했던 게 여기서 터진다.

### 4.3 ⚠️ newFixedThreadPool의 정체

원문에 나온 등식이다.

```java
Executors.newFixedThreadPool(n)
    == new ThreadPoolExecutor(n, n, 0, TimeUnit.MILLISECONDS, new LinkedBlockingQueue<>())
                                                              // ⚠️ 무한 큐
```

| 장점 | 위험 |
| --- | --- |
| 스레드 수가 n개로 고정 → CPU·메모리 예측 가능 | 작업이 처리 속도보다 빨리 쌓이면 **큐가 무한히 커진다** |
| 설정이 간단 | 결국 **OOM**. 그리고 응답 지연이 끝없이 늘어난다 |

실무에서는 `ThreadPoolExecutor`를 **직접 생성하고 큐 크기를 명시**하는 경우가 많다. 앞 글의 **"한정된 버퍼가 백프레셔를 만든다"**가 스레드 풀에서도 그대로 적용된다.

---

## 5. Callable과 Future

### 5.1 Runnable로 결과를 받으려면

```java
static class MyRunnable implements Runnable {
    int value;                              // ⚠️ 결과를 담을 필드

    @Override
    public void run() {
        sleep(2000);
        value = new Random().nextInt(10);
    }
}

MyRunnable task = new MyRunnable();
Thread thread = new Thread(task);
thread.start();
thread.join();                              // 끝날 때까지 기다리고
int result = task.value;                    // 필드에서 꺼낸다
```

원문 지적대로 **결과를 작업 객체 어딘가에 저장해야 한다.** 앞 글 "스레드 제어와 생명 주기"의 `SumTask` + `join()`과 똑같은 패턴이다. 그때 9장에서 "이 패턴의 한계와 `ExecutorService` + `Future`로의 발전"을 미리 봤는데, 여기서 실현된다.

### 5.2 Callable

```java
public interface Callable<V> {
    V call() throws Exception;
}
```

| | `Runnable` | `Callable<V>` |
| --- | --- | --- |
| 메서드 | `void run()` | `V call() throws Exception` |
| 반환값 | ❌ | ✅ **제네릭 타입 V** |
| 체크 예외 | ❌ | ✅ **던질 수 있다** |

### 5.3 사용

```java
ExecutorService es = Executors.newFixedThreadPool(1);
Future<Integer> future = es.submit(new MyCallable());
Integer result = future.get();              // ⭐ 결과를 "받는다"
log("result value = " + result);
es.close();
```

```java
static class MyCallable implements Callable<Integer> {
    @Override
    public Integer call() throws Exception {
        sleep(2000);
        return new Random().nextInt(10);    // ⭐ 그냥 return
    }
}
```

🔴 **오타**: `new Threadpool**E**xecutor` → **`ThreadPoolExecutor`**, "훨**신**" → **훨씬**

### 5.4 💡 같은 문제, 두 가지 풀이

앞 글의 1~100 합계를 두 방식으로 비교하면 차이가 선명하다.

| | `Thread` + `join()` (앞 글) | `ExecutorService` + `Future` (이번 글) |
| --- | --- | --- |
| 결과 전달 | 공유 필드 `task.result` | ⭐ **반환값** `future.get()` |
| 기다리기 | `thread.join()` | `future.get()` |
| 체크 예외 | `try-catch` 필수 | ⭐ **`throws Exception`으로 그냥 던짐** |
| 예외 전달 | ❌ main이 못 받음 | ⭐ `ExecutionException`으로 받음 |
| 스레드 관리 | 직접 생성·시작 | ⭐ 풀이 알아서 |
| 코드의 느낌 | 스레드를 다룬다 | **함수를 호출하는 것 같다** |

원문의 **"단순한 싱글스레드 방식으로 개발하는 느낌"**이 정확한 표현이다.

---

## 6. Future의 동작 원리

### 6.1 왜 결과 대신 Future를 주나

`submit()`을 호출하는 순간 작업은 **아직 실행되지 않았을 수도 있다.** 풀의 다른 스레드가 **미래의 어느 시점에** 실행하므로, 결과를 즉시 돌려주는 건 불가능하다. 그래서 **결과를 나중에 받을 수 있는 "교환권"**을 준다. 그게 `Future`다.

### 6.2 내부 흐름

```text
① es.submit(taskA)
        │
        ▼
② FutureTask 생성 (taskA 를 감싼다)
   ┌─────────────────────────────┐
   │ FutureTask                   │
   │   callable = taskA           │
   │   state    = NEW             │   ← Not completed
   │   outcome  = (없음)           │
   └─────────────────────────────┘
        │
        ├──▶ ③ 블로킹 큐에 넣는다 (⭐ taskA 가 아니라 FutureTask 를!)
        │
        └──▶ ④ FutureTask 참조를 즉시 반환  ← submit() 은 여기서 끝
                    │
                    ▼
               요청 스레드는 계속 자기 일을 한다

   ... 그 사이 ...

⑤ 풀 스레드가 큐에서 FutureTask 를 꺼내 run() 실행
        │
        ▼
⑥ FutureTask.run() → taskA.call() → 결과
        │
        ▼
⑦ outcome = 결과, state = COMPLETED
        │
        ▼
⑧ get() 으로 대기 중인 스레드가 있으면 깨운다
```

```text
future 즉시 반환, future = FutureTask@6576fe71[Not completed, task = ...MyCallable@7eda2dbb]
...
future 완료, future = FutureTask@6576fe71[Completed normally]
```

**같은 객체(`@6576fe71`)의 상태가 바뀐 것**이다.

🔴 **오타**: "**브로킹** 큐" → **블로킹**, "함**게** 구현" → **함께**

### 6.3 FutureTask가 큐에 들어갈 수 있는 이유

```text
FutureTask implements RunnableFuture
RunnableFuture extends Runnable, Future
```

**`FutureTask`는 `Runnable`이면서 `Future`다.** 스레드 풀 입장에서는 그냥 `Runnable` 하나를 실행하는 것이고, 요청 스레드 입장에서는 `Future`로 결과를 기다린다. 한 객체가 **양쪽에 다른 얼굴**을 보여주는 셈이다.

### 6.4 get()을 호출하면

| Future 상태 | `get()`의 동작 |
| --- | --- |
| 완료 | **즉시** 결과 반환 |
| 미완료 | 완료될 때까지 **블로킹** (`RUNNABLE` → `WAITING`) |

🟡 **원문의 "요청 스레드가 마치 락을 얻을 때처럼 대기"를 정확히 하면:** `synchronized` 락 대기는 `BLOCKED`지만, `future.get()` 대기는 **`WAITING`**이다. 내부적으로 앞 글의 **`LockSupport.park()`**를 쓰기 때문이다. 작업을 끝낸 풀 스레드가 `unpark()`로 깨운다.

```text
main:    future.get() ──▶ LockSupport.park()   WAITING
                                    ▲
pool-1:  call() 완료 → 결과 저장 → LockSupport.unpark(main)
```

💡 **`Thread.join()`, `Future.get()` 같은 메서드를 블로킹 메서드라고 한다.** 호출한 스레드가 다른 작업이 끝날 때까지 멈춘다.

### 6.5 ⭐ Future.get()도 가시성을 보장한다

앞 글에서 **"`join()`은 기다림만 주는 게 아니라 가시성도 준다"**고 했다. `Future.get()`도 마찬가지다. 공식 문서에 명시된 규칙이다.

```text
submit() 이전의 요청 스레드 작업
        │  happens-before
        ▼
작업(call) 안의 모든 동작
        │  happens-before
        ▼
future.get() 반환 이후의 요청 스레드 작업
```

그래서 `call()` 안에서 만든 객체를 `get()`으로 받으면, **그 객체의 내부 상태까지 전부 최신으로 보인다.** `volatile`을 붙일 필요가 없다.

---

## 7. ⭐ 왜 submit과 get을 분리했나

원문에서 스스로 던진 질문이다.

> "그냥 `es.submit()`에서 바로 결과를 반환하면 안 되나? 어차피 `get()`에서 블로킹될 텐데?"

### 7.1 만약 submit이 결과를 바로 반환한다면

```text
시간 ──────────────────────────────────────────▶
        0s          1s          2s          3s          4s

main   │ submit(task1) 블로킹 ─────▶│ submit(task2) 블로킹 ─────▶│
pool-1 │■■■■■ task1 ■■■■■■■■■■■■■■│
pool-2 │   (놀고 있음)               │■■■■■ task2 ■■■■■■■■■■■■■■│

⚠️ 총 4초 — 스레드가 2개인데 하나씩만 일한다. 사실상 단일 스레드.
```

### 7.2 Future를 반환하니까

```text
시간 ──────────────────────────────────────────▶
        0s          1s          2s

main   │submit ×2│ get(f1) 대기 ───▶│get(f2) 즉시│
pool-1 │■■■■■ task1 ■■■■■■■■■■■■■■■│
pool-2 │■■■■■ task2 ■■■■■■■■■■■■■■■│

✅ 총 2초 — 두 작업이 동시에 돈다
```

`future1.get()`에서 2초를 기다리는 동안 task2도 이미 돌고 있었으므로, `future2.get()`은 **기다릴 필요 없이 즉시** 반환된다. 앞 글의 `join()`에서 정리한 **"대기 시간은 `t1 + t2`가 아니라 `max(t1, t2)`"**와 같은 원리다.

> ⭐ **submit은 "시작해줘", get은 "결과 줘"다.**
> 둘을 분리했기 때문에 **여러 작업을 먼저 다 시작시켜 놓고**, 결과는 필요할 때 모을 수 있다.

### 7.3 ⚠️ 잘못 쓰면 분리한 의미가 사라진다

```java
// ❌ 제출하자마자 기다림 — 4초
Future<Integer> future1 = es.submit(task1);
future1.get();                               // 여기서 2초 멈춤. task2 는 아직 제출도 안 됨
Future<Integer> future2 = es.submit(task2);
future2.get();

// ❌ 같은 실수의 축약형 — 4초
es.submit(task1).get();
es.submit(task2).get();

// ✅ 제출은 몰아서, 기다림은 나중에 — 2초
Future<Integer> future1 = es.submit(task1);
Future<Integer> future2 = es.submit(task2);
Integer r1 = future1.get();
Integer r2 = future2.get();
```

🔴 **원문 코드 오타**: `funture1.get()` → **`future1`**. 그리고 원문 코드는 `es.submit(task1);`의 반환값을 **변수에 담지 않고** `future1.get()`을 호출해서 컴파일되지 않는다. 위처럼 `Future<Integer> future1 = ...`로 받아야 한다.

### 7.4 SumTask 예제

```java
ExecutorService es = Executors.newFixedThreadPool(2);
Future<Integer> future1 = es.submit(new SumTask(1, 50));
Future<Integer> future2 = es.submit(new SumTask(51, 100));

Integer result1 = future1.get();
Integer result2 = future2.get();
log("result = " + (result1 + result2));     // 5050
```

```java
static class SumTask implements Callable<Integer> {
    @Override
    public Integer call() throws Exception {
        Thread.sleep(2000);                  // ⭐ try-catch 없이 그냥 씀
        int sum = 0;
        for (int i = startValue; i <= endValue; i++) sum += i;
        return sum;
    }
}
```

🔴 **원문 오타 두 개**
- 로그 문자열 `"taks1.result"` → **`task1`**
- 설명의 "`run() throws Exception`" → **`call() throws Exception`**. `Callable`의 메서드는 `call()`이다

---

## 8. Future API와 취소

### 8.1 인터페이스

```java
public interface Future<V> {
    boolean cancel(boolean mayInterruptIfRunning);
    boolean isCancelled();
    boolean isDone();

    V get() throws InterruptedException, ExecutionException;
    V get(long timeout, TimeUnit unit)                     // 🔴 원문은 소문자 v
        throws InterruptedException, ExecutionException, TimeoutException;

    // Java 19+
    enum State { RUNNING, SUCCESS, FAILED, CANCELLED }
    default State state() { ... }
    default V resultNow() { ... }
    default Throwable exceptionNow() { ... }
}
```

| 메서드 | 기능 | 비고 |
| --- | --- | --- |
| `get()` | 완료까지 대기 후 결과 | 무한 대기 가능 ⚠️ |
| `get(timeout, unit)` | 제한 시간만큼만 대기 | 초과 시 `TimeoutException` |
| `cancel(bool)` | 취소 시도 | 8.3 참고 |
| `isCancelled()` | 취소됐나 | |
| `isDone()` | 끝났나 | ⚠️ 8.2 참고 |
| `state()` | `RUNNING` / `SUCCESS` / `FAILED` / `CANCELLED` | Java 19+ |
| `resultNow()` | 완료된 결과를 **블로킹 없이** 꺼냄 | Java 19+. 미완료면 예외 |
| `exceptionNow()` | 실패한 예외를 **블로킹 없이** 꺼냄 | Java 19+ |

🔴 **오타**: 인터페이스 코드의 `v get(long timeout, ...)` → **`V`**, "상**탤**를 반환" → **상태를**

💡 **`get(timeout, unit)`을 기본으로 쓰는 습관을 들이자.** 인자 없는 `get()`은 작업이 영원히 안 끝나면 요청 스레드도 영원히 멈춘다. 앞 글에서 `join()` 대신 `join(ms)`를 쓰던 것과 같은 이유다.

### 8.2 ⚠️ isDone()은 "성공"이 아니다

원문이 굵게 강조한 부분이 정확하다.

| 끝난 방식 | `isDone()` | `isCancelled()` | `state()` |
| --- | --- | --- | --- |
| 정상 완료 | `true` | `false` | `SUCCESS` |
| 예외로 종료 | ⚠️ **`true`** | `false` | `FAILED` |
| 취소됨 | ⚠️ **`true`** | `true` | `CANCELLED` |

**`isDone() == true`를 보고 "성공했구나"라고 판단하면 안 된다.** 성공 여부는 `get()`을 호출해서 예외가 나는지 보거나, Java 19+라면 `state()`로 확인한다.

### 8.3 취소 — cancel(false)

```java
Future<String> future = es.submit(new MyTask());   // 1초마다 "작업 중" 출력, 10회
sleep(3000);
boolean cancelResult = future.cancel(false);
future.get();                                       // → CancellationException
```

```text
[pool-1-thread-1] 작업 중: 0
[pool-1-thread-1] 작업 중: 1
[pool-1-thread-1] 작업 중: 2
[     main] future.cancel(false) 호출
[     main] cancel(false) result: true
[     main] Future는 이미 취소되었습니다.
[pool-1-thread-1] 작업 중: 3        ⚠️ 작업은 계속 돈다
[pool-1-thread-1] 작업 중: 4
...
[pool-1-thread-1] 작업 중: 9
```

🟡 **`cancel(false)`는 "결과를 받지 않겠다"는 선언일 뿐 작업을 멈추지 않는다.** Future는 취소 상태가 되고 `get()`은 `CancellationException`을 던지지만, 풀 스레드는 끝까지 일한다. 앞 글에서 본 **`join(ms)` 타임아웃과 같은 성격** — 기다리기를 포기할 뿐 상대를 중단시키지 않는다.

### 8.4 취소 — cancel(true)

```text
[pool-1-thread-1] 작업 중: 2
[     main] future.cancel(true) 호출
[pool-1-thread-1] 인터럽트 발생           ⭐ 작업이 멈췄다
[     main] cancel(true) result: true
[     main] Future는 이미 취소되었습니다.
```

> ⭐ **`cancel(true)`의 정체는 결국 `Thread.interrupt()`다.**
> 앞 글 "인터럽트"에서 정리한 원칙이 그대로 적용된다.

| 작업이 이렇게 되어 있다면 | `cancel(true)` 결과 |
| --- | --- |
| `sleep()` / `wait()` / `take()` 등으로 대기 중 | ✅ `InterruptedException`으로 깨어남 |
| 루프에서 `Thread.interrupted()`를 확인 | ✅ 멈춤 |
| CPU 연산 루프만 돌고 확인 안 함 | ❌ **안 멈춘다** |
| 블로킹 I/O (`socket.read()`) 중 | ❌ **안 멈춘다** |
| `InterruptedException`을 잡고 **삼킨다** | ❌ **안 멈춘다** |

**취소는 협력적이다.** 작업 쪽에서 인터럽트에 반응하도록 짜여 있어야 한다.

### 8.5 💡 취소된 작업의 반환값은 버려진다

```java
} catch (InterruptedException e) {
    log("인터럽트 발생");
    return "Interrupted";        // ⚠️ 이 값은 아무도 받지 못한다
}
```

Future는 **이미 `CANCELLED` 상태로 확정**됐으므로, 작업이 뒤늦게 `"Interrupted"`를 반환해도 무시된다. `get()`은 여전히 `CancellationException`이다.

---

## 9. 예외 처리

### 9.1 ExecutionException

```java
static class ExCallable implements Callable<Integer> {
    @Override
    public Integer call() throws Exception {
        throw new IllegalStateException("ex!");
    }
}

try {
    future.get();
} catch (ExecutionException e) {
    log("e = " + e);
    log("cause = " + e.getCause());          // ⭐ 원본 예외
}
```

```text
[     main] future.get() 호출 시도, future.state(): FAILED
[     main] e = java.util.concurrent.ExecutionException: java.lang.IllegalStateException: ex!
[     main] cause = java.lang.IllegalStateException: ex!
```

```text
작업 스레드: call() 에서 IllegalStateException 발생
                   │
                   ▼
      FutureTask 가 잡아서 내부에 저장 (state = FAILED)
                   │
                   ▼
요청 스레드: get() 호출 → ExecutionException 으로 감싸서 던짐
                   │
                   ▼
            e.getCause() → 원본 IllegalStateException
```

🔴 **원문 설명 오류**: "**`e.getClass()`**을 호출하면 작업에서 발생한 원본 예외를 받을 수 있음" → **`e.getCause()`**다. 코드는 `getCause()`를 제대로 쓰고 있는데 설명만 틀렸다. `getClass()`는 `ExecutionException.class`를 돌려줄 뿐이다.

### 9.2 ⭐ 첫 글의 한계가 여기서 풀린다

첫 글에서 이렇게 정리했다.

> "`run()`에서 예외가 나면 그 스레드만 죽고, **main은 이 예외를 절대 catch할 수 없다** — 스택이 다르므로."

`Future`가 이 문제를 해결한다. 다른 스레드의 스택에서 터진 예외를 **`FutureTask`가 붙잡아 보관**했다가, **요청 스레드의 `get()` 호출 시점에 다시 던져준다.** 스택이 달라서 전달할 길이 없던 예외에 **우회로**를 만든 것이다.

### 9.3 ⚠️ execute()와 submit()의 예외는 완전히 다르게 처리된다 💡

실무에서 정말 많이 당하는 함정이다.

```java
es.execute(() -> { throw new RuntimeException("펑"); });
es.submit(() -> { throw new RuntimeException("펑"); });
```

| | `execute(Runnable)` | `submit(...)` |
| --- | --- | --- |
| 예외 발생 시 | 스레드 밖으로 전파 | ⭐ **Future 안에 저장** |
| 콘솔 출력 | ✅ 스택 트레이스가 찍힌다 | ❌ **아무것도 안 찍힌다** |
| 풀 스레드 | 죽고, 풀이 **새 스레드로 교체** | 멀쩡히 계속 쓰임 |
| 예외를 확인하는 법 | `UncaughtExceptionHandler` | `future.get()` |

> ⚠️ **`submit()`으로 제출하고 `get()`을 안 부르면, 예외가 조용히 사라진다.**
> 로그 한 줄 없이 작업이 실패해 있는 상황이 된다. 결과가 필요 없는 작업이라도, `submit()`을 썼다면 `get()`으로 확인하거나 작업 안에서 직접 `try-catch`로 로그를 남겨야 한다.

---

## 10. invokeAll과 invokeAny

### 10.1 원문에 빠진 CallableTask

실행 로그로부터 이런 작업임을 알 수 있다.

```java
public class CallableTask implements Callable<Integer> {

    private final String name;
    private int sleepMs = 1000;

    public CallableTask(String name) { this.name = name; }
    public CallableTask(String name, int sleepMs) { this.name = name; this.sleepMs = sleepMs; }

    @Override
    public Integer call() throws Exception {
        log(name + "실행");
        sleep(sleepMs);          // 인터럽트되면 "인터럽트 발생, ..." 로그를 남기는 유틸
        log(name + "완료");
        return sleepMs;
    }
}
```

### 10.2 invokeAll — 전부 기다린다

```java
List<CallableTask> tasks = List.of(
        new CallableTask("task1", 1000),
        new CallableTask("task2", 2000),
        new CallableTask("task3", 3000));

List<Future<Integer>> futures = es.invokeAll(tasks);   // ⭐ 여기서 3초 블로킹
for (Future<Integer> future : futures) {
    log("value = " + future.get());                    // 이미 끝났으니 즉시 반환
}
```

```text
[pool-1-thread-1] task1실행
[pool-1-thread-2] task2실행
[pool-1-thread-3] task3실행
[pool-1-thread-1] task1완료
[pool-1-thread-2] task2완료
[pool-1-thread-3] task3완료          ← 가장 느린 작업이 끝나야
[     main] value = 1000            ← 그제서야 반환
[     main] value = 2000
[     main] value = 3000
```

💡 `invokeAll()`이 반환한 시점에는 모든 Future가 완료 상태라서 `get()`은 블로킹하지 않는다. 다만 **작업이 예외로 끝났다면 `get()`에서 `ExecutionException`이 여전히 나온다.** 완료 ≠ 성공(8.2)이다.

🟡 **타임아웃 버전 보강**: `invokeAll(tasks, timeout, unit)`은 시간이 초과되면 **완료되지 않은 작업을 취소**하고 반환한다. 원문에는 이 동작이 빠져 있다.

### 10.3 invokeAny — 첫 성공만 받는다

```java
Integer value = es.invokeAny(tasks);
```

```text
[pool-1-thread-1] task1실행
[pool-1-thread-2] task2실행
[pool-1-thread-3] task3실행
[pool-1-thread-1] task1완료
[pool-1-thread-2] 인터럽트 발생, sleep interrupted     ⭐ 나머지는 취소
[pool-1-thread-3] 인터럽트 발생, sleep interrupted
[     main] value = 1000
```

🔴 **원문의 "가장 먼저 완료된 작업의 결과"를 정확히 하면 "가장 먼저 성공한 작업의 결과"다.** 공식 문서가 **"예외 없이 완료된"** 작업이라고 명시한다. 가장 빠른 작업이 예외로 끝나면 무시하고 **다음 성공을 기다린다.** 전부 실패하면 `ExecutionException`이다.

💡 나머지 작업이 "인터럽트 발생"을 찍은 건 `invokeAny()`가 내부적으로 **`cancel(true)`**를 호출하기 때문이다. 8.4의 원리가 그대로 쓰였다.

💡 **용도**: 같은 데이터를 여러 서버(미러)에 동시에 요청하고 **가장 빨리 온 응답만 쓰는** 경우. 나머지는 자동으로 취소된다.

| | `invokeAll` | `invokeAny` |
| --- | --- | --- |
| 기다리는 대상 | **전부** | **첫 성공 하나** |
| 반환 | `List<Future<T>>` | `T` (결과 그 자체) |
| 나머지 작업 | — | ⭐ **취소** (`cancel(true)`) |
| 걸리는 시간 | 가장 **느린** 작업 | 가장 **빠른 성공** 작업 |

---

## 11. ⚠️ 스레드 풀은 반드시 닫아야 한다 💡

### 11.1 🔴 원문의 취소·예외 예제에는 close()가 없다

```java
public static void main(String[] args) {
    ExecutorService es = Executors.newFixedThreadPool(1);
    Future<String> future = es.submit(new MyTask());
    ...
    // ⚠️ es.close() 가 없다
}
```

`FutureCancelMain`과 `FutureExceptionMain` 모두 `es.close()`가 빠져 있다. 그래서 **main이 끝나도 프로그램이 종료되지 않는다.**

### 11.2 이유 — 풀 스레드는 사용자 스레드다

첫 글에서 정리한 규칙이 그대로 적용된다.

> **사용자 스레드가 하나라도 남아 있으면 JVM은 종료되지 않는다.**

```text
main 종료
   │
   ▼
pool-1-thread-1 은? → 사용자 스레드 (기본 ThreadFactory 는 non-daemon 으로 만든다)
   │
   ▼
할 일이 없으니 BlockingQueue.take() 에서 WAITING
   │
   ▼
⚠️ 영원히 기다린다 → JVM 이 끝나지 않는다
```

`newFixedThreadPool`의 core 스레드는 **일이 없어도 사라지지 않는다**(keepAliveTime은 core 초과분에만 적용). 그래서 명시적으로 닫아야 한다.

### 11.3 닫는 방법

**Java 19+**

```java
// ✅ try-with-resources — AutoCloseable 이므로
try (ExecutorService es = Executors.newFixedThreadPool(2)) {
    Future<Integer> f = es.submit(task);
    log(f.get());
}   // ⭐ 자동으로 close()
```

`close()`는 **새 작업을 거부하고, 이미 제출된 작업이 끝날 때까지 기다린 뒤** 반환한다.

**Java 18 이하**

```java
es.shutdown();                                   // 새 작업 거부, 기존 작업은 계속
try {
    if (!es.awaitTermination(10, TimeUnit.SECONDS)) {
        es.shutdownNow();                        // 실행 중인 작업에 interrupt
    }
} catch (InterruptedException e) {
    es.shutdownNow();
    Thread.currentThread().interrupt();          // ⭐ 인터럽트 상태 복원
}
```

| 메서드 | 새 작업 | 큐의 작업 | 실행 중 작업 | 기다리나 |
| --- | --- | --- | --- | --- |
| `shutdown()` | 거부 | 처리함 | 계속 | ❌ 즉시 반환 |
| `shutdownNow()` | 거부 | **버리고 반환** | `interrupt()` | ❌ 즉시 반환 |
| `awaitTermination()` | — | — | — | ✅ 종료까지 대기 |
| `close()` (19+) | 거부 | 처리함 | 계속 | ✅ **끝날 때까지** |

---

## 12. 원문 점검 결과 요약

### 🔴 정정

| # | 원문 | 정정 |
| --- | --- | --- |
| 1 | ⭐ "`e.getClass()`을 호출하면 원본 예외" | **`e.getCause()`**. 코드는 맞고 설명만 틀림 |
| 2 | ⭐ 취소·예외 예제에 `es.close()` 없음 | 풀 스레드가 남아 **프로그램이 종료되지 않음** |
| 3 | invokeAny "가장 먼저 **완료된**" | 가장 먼저 **성공한**. 예외로 끝난 건 무시 |
| 4 | `es.submit(task1); funture1.get();` | 반환값을 변수에 안 담음 + `funture` 오타 |
| 5 | "`run() throws Exception`" | **`call()`** |
| 6 | `v get(long timeout, ...)` | **`V`** |
| 7 | `ThreadpoolExecutor` | **`ThreadPoolExecutor`** |
| 8 | `ex.execute(작업)` | **`es`** |
| 9 | 로그 `"taks1.result"` | **`task1`** |
| 10 | 때**가**지는 / 이 시간**이** 동안 / 훨**신** / **브로킹** / 함**게** / 상**탤** | 때까지는 / 이 시간 동안 / 훨씬 / 블로킹 / 함께 / 상태 |

### 🟡 보강

| # | 항목 | 보강 내용 |
| --- | --- | --- |
| 1 | `close()` / `AutoCloseable` | **Java 19+**. 이전엔 `shutdown()` + `awaitTermination()` |
| 2 | "스레드 하나는 1MB 이상" | 기본 1MB는 **예약 크기**. 실제 물리 메모리는 쓰는 만큼 |
| 3 | `getActiveCount()` 등 | **근사값**. 모니터링용 |
| 4 | "taskC부터는 재사용" | taskC는 **큐로** 들어가고, 재사용은 스레드가 꺼낼 때 |
| 5 | "마치 락을 얻을 때처럼 대기" | `BLOCKED`가 아니라 **`WAITING`** (`LockSupport.park`) |
| 6 | `cancel(false)` 결과 | **작업은 끝까지 돈다.** 결과를 버릴 뿐 |
| 7 | `invokeAll(timeout)` | 초과 시 **미완료 작업을 취소** |
| 8 | `isDone()` | 원문이 잘 강조함. **성공과 무관**하다는 점을 표로 정리 |

### 💡 추가

| # | 내용 | 위치 |
| --- | --- | --- |
| 1 | ⭐ **풀 스레드의 대기 = `BlockingQueue.take()`** — 생산자-소비자의 완성품 | 2.2 |
| 2 | ⭐ **스레드 생성 규칙** (core → 큐 → max → 거절) | 4.1 |
| 3 | ⭐ **무한 큐면 `maximumPoolSize`는 장식** | 4.2 |
| 4 | `newFixedThreadPool`의 무한 큐 → OOM 위험 | 4.3 |
| 5 | `Thread` + `join()` vs `Future` 비교 | 5.4 |
| 6 | `FutureTask`는 `Runnable`이자 `Future` | 6.3 |
| 7 | ⭐ **`Future.get()`도 happens-before 보장** | 6.5 |
| 8 | 제출은 몰아서, `get()`은 나중에 — `max(t1, t2)` | 7.2 |
| 9 | `resultNow()` / `exceptionNow()` (Java 19+) | 8.1 |
| 10 | ⭐ **`cancel(true)` = `interrupt()`** — 협력하지 않으면 안 멈춘다 | 8.4 |
| 11 | 취소된 작업의 반환값은 버려진다 | 8.5 |
| 12 | ⭐ **main이 못 잡던 예외를 Future가 전달해준다** | 9.2 |
| 13 | ⭐ **`submit()`은 `get()` 안 하면 예외가 사라진다** | 9.3 |
| 14 | 누락된 `CallableTask` 클래스 | 10.1 |
| 15 | ⭐ **풀 스레드는 non-daemon → 안 닫으면 JVM이 안 끝남** | 11.2 |
| 16 | try-with-resources / `shutdown` 계열 비교 | 11.3 |

---

## 13. 최종 정리

### 한 장으로 보기

```text
                         ExecutorService
 ┌──────────────────────────────────────────────────────────┐
 │                                                          │
 │  submit(Callable) ─▶ FutureTask 로 감싸서 ─▶ BlockingQueue │
 │        │                                       │         │
 │        │ 즉시 반환                       take() │         │
 │        ▼                                       ▼         │
 │     Future ◀──── 결과 / 예외를 저장 ──── 풀 스레드 (재사용) │
 │        │                                                 │
 └────────┼─────────────────────────────────────────────────┘
          │
      get()  ─ 완료: 즉시 반환
             ─ 미완료: WAITING 으로 대기
             ─ 실패: ExecutionException (getCause 로 원본)
```

### 핵심 개념

| 개념 | 한 줄 요약 |
| --- | --- |
| **스레드 풀** | 스레드를 **재사용**하고 **수를 제한**한다. 정체는 생산자-소비자 |
| **놀고 있는 풀 스레드** | `BlockingQueue.take()`에서 `WAITING` |
| **스레드 생성 순서** | core까지 생성 → **큐** → 큐가 차야 max까지 → 거절 |
| **Callable** | 반환값 + 체크 예외 |
| **Future** | 작업의 **미래 결과를 담는 교환권** (구현체 `FutureTask`) |
| **submit / get** | submit은 **즉시**, get은 **블로킹** |
| **예외** | `ExecutionException`으로 감싸져 `get()`에서 나온다 |
| **cancel(true)** | 결국 `interrupt()`. 작업이 협력해야 멈춘다 |
| **invokeAll / Any** | 전부 기다림 / **첫 성공**만 받고 나머지 취소 |
| **종료** | 풀 스레드는 사용자 스레드 → **반드시 `close()`** |

### 이것만은 기억

> **① 제출은 몰아서, `get()`은 나중에** — 그래야 병렬로 돈다
> **② `submit()`의 예외는 `get()`을 부르기 전까지 보이지 않는다**
> **③ 무한 큐를 쓰면 `maximumPoolSize`는 동작하지 않는다**
> **④ 스레드 풀은 반드시 닫는다**