# [Java] Condition으로 대기 공간 나누기, 그리고 BlockingQueue

이전 글에서 `wait/notify`로 생산자-소비자 문제를 풀었다. 그런데 찜찜한 게 남았다.

```text
16:35:52.857 [consumer1] [take] 소비자 데이터 획득, notify() 호출
16:35:52.857 [consumer2] [take] 소비자 깨어남
16:35:52.857 [consumer2] [take] 큐에 데이터가 없음. 소비자 대기   ← 헛수고
```

**소비자가 소비자를 깨웠다.** 깨워야 할 건 생산자였는데.

원인은 명확하다. 생산자와 소비자가 **같은 대기 집합**을 쓰기 때문이다. `synchronized`의 대기 집합은 객체당 딱 하나라 쪼갤 방법이 없다. 그래서 `ReentrantLock`으로 넘어간다.

---

## 다루는 내용

- `Condition` — 직접 만드는 대기 공간
- ⭐ 생산자/소비자 대기 공간 분리
- `notify()`와 `signal()`의 정밀한 차이
- 스레드 대기 방식 총정리
- `lock()` vs `lockInterruptibly()`
- `BlockingQueue`와 4가지 선택지
- ⚠️ 실무에서 자주 밟는 지뢰

---

# 1. Condition 도입

## 1.1 해결 방향

```text
생산자 스레드는 데이터를 생성하고 → 대기 중인 "소비자"에게 알린다
소비자 스레드는 데이터를 소비하고 → 대기 중인 "생산자"에게 알린다

⭐ 생산자용 / 소비자용 대기 집합을 따로 관리할 수 있으면 된다
```

`Object`의 대기 집합은 객체에 기본으로 하나 달려 있고 늘릴 수 없다. `ReentrantLock`은 **대기 공간을 직접 만들어 쓴다.**

## 1.2 코드

```java
public class BoundedQueueV4 implements BoundedQueue {

    private final Lock lock = new ReentrantLock();
    private final Condition condition = lock.newCondition();   // ⭐ 대기 공간 생성

    private final Queue<String> queue = new ArrayDeque<>();
    private final int max;

    @Override
    public void put(String data) {
        lock.lock();
        try {
            while (queue.size() == max) {
                log("[put] 큐가 가득 참. 대기: " + data);
                try {
                    condition.await();              // ⭐ 락 반납 + 대기
                    log("[put] 생산자 깨어남");
                } catch (InterruptedException e) {
                    throw new RuntimeException(e);
                }
            }
            queue.offer(data);
            log("[put] 생산자 데이터 저장, signal() 호출");
            condition.signal();
        } finally {
            lock.unlock();
        }
    }
    // take() 도 대칭
}
```

| 메서드 | 대응하는 `Object` 메서드 | 동작 |
| --- | --- | --- |
| `lock.newCondition()` | — | 대기 공간을 만든다 |
| `condition.await()` | `wait()` | **락을 반납하고** 이 공간에서 대기 |
| `condition.signal()` | `notify()` | 이 공간의 스레드 하나를 깨운다 |

> ⚠️ `Condition`에는 `wait()`라는 메서드가 없다. **`await()`**다. `Object.wait()`와 헷갈리지 않게 일부러 다른 이름을 썼다.

## 1.3 그런데 V4는 아직 해결이 아니다

**Condition을 하나만 만들었으므로 `wait/notify` 버전과 동작이 사실상 같다.** 생산자와 소비자가 여전히 같은 대기 공간을 쓴다. V4는 **도구를 바꾼 단계**이고, 실제 해결은 다음이다.

---

# 2. Condition의 API

## 2.1 Object의 것과 대응

| `Object` (모니터) | `Condition` | 차이 |
| --- | --- | --- |
| `wait()` | `await()` | |
| `wait(ms)` | `await(time, unit)` | 시간 단위 지정 가능 |
| — | `awaitNanos(nanos)` | ⭐ **남은 시간을 반환** |
| — | `awaitUninterruptibly()` | ⭐ **인터럽트 무시** |
| — | `awaitUntil(Date)` | 절대 시각 |
| `notify()` | `signal()` | |
| `notifyAll()` | `signalAll()` | |
| **객체당 1개 (고정)** | ⭐ **락당 여러 개** | 핵심 차이 |

## 2.2 awaitNanos()가 남은 시간을 반환하는 이유

```java
long remaining = TimeUnit.SECONDS.toNanos(5);
while (!조건()) {
    if (remaining <= 0) return false;              // 진짜 타임아웃
    remaining = condition.awaitNanos(remaining);   // ⭐ 남은 시간만큼만 다시 대기
}
```

`wait(5000)`은 spurious wakeup으로 깨어나면 **다시 5초를 처음부터 기다린다.** 총 대기 시간이 5초를 넘어버린다. `awaitNanos()`는 남은 시간을 돌려주므로 **전체 타임아웃을 정확히 지킬 수 있다.**

## 2.3 제약

```java
lock.lock();
try {
    condition.await();     // ✅ 락을 쥔 상태
} finally {
    lock.unlock();
}

condition.await();         // 💥 IllegalMonitorStateException
```

**`Condition`은 자기를 만든 `Lock`에 종속적이다.** `wait()`가 `synchronized` 안에서만 되는 것과 같은 원리다.

---

# 3. ⭐ 대기 공간 분리

## 3.1 코드

```java
public class BoundedQueueV5 implements BoundedQueue {

    private final Lock lock = new ReentrantLock();
    private final Condition producerCond = lock.newCondition();   // 생산자 대기실
    private final Condition consumerCond = lock.newCondition();   // 소비자 대기실

    private final Queue<String> queue = new ArrayDeque<>();
    private final int max;

    @Override
    public void put(String data) {
        lock.lock();
        try {
            while (queue.size() == max) {
                log("[put] 큐가 가득 참. 대기: " + data);
                try {
                    producerCond.await();      // 생산자는 여기서 대기
                    log("[put] 생산자 깨어남");
                } catch (InterruptedException e) {
                    throw new RuntimeException(e);
                }
            }
            queue.offer(data);
            log("[put] 생산자 데이터 저장, signal() 호출");
            consumerCond.signal();             // ⭐ 소비자만 정확히 깨운다
        } finally {
            lock.unlock();
        }
    }

    @Override
    public String take() {
        lock.lock();
        try {
            while (queue.isEmpty()) {
                log("[take] 큐에 데이터가 없음. 소비자 대기");
                try {
                    consumerCond.await();      // 소비자는 여기서 대기
                    log("[take] 소비자 깨어남");
                } catch (InterruptedException e) {
                    throw new RuntimeException(e);
                }
            }
            String data = queue.poll();
            log("[take] 소비자 데이터 획득, signal() 호출");
            producerCond.signal();             // ⭐ 생산자만 정확히 깨운다
            return data;
        } finally {
            lock.unlock();
        }
    }
}
```

## 3.2 구조

```text
   ┌──────────────── ReentrantLock (락은 여전히 1개) ────────────────┐
   │                                                                │
   │   🔒 lock                                                       │
   │                                                                │
   │   💤 producerCond   "자리 나면 깨워줘"                           │
   │      [producer3] [producer4]                                   │
   │                                                                │
   │   💤 consumerCond   "데이터 오면 깨워줘"                         │
   │      [consumer1] [consumer2]                                   │
   │                                                                │
   └────────────────────────────────────────────────────────────────┘

   생산자가 데이터를 넣으면 → consumerCond.signal()  ✅ 소비자만 깨운다
   소비자가 데이터를 빼면  → producerCond.signal()  ✅ 생산자만 깨운다
```

> ⭐ **핵심 아이디어: 생산자는 소비자를 깨우고, 소비자는 생산자를 깨운다.**
> 자기 편을 깨우는 헛수고가 구조적으로 사라진다.

## 3.3 여기서 signal()이 안전한 이유

이전 글에서 `notify()`가 위험하다고 했는데, 여기서는 왜 `signal()`로 충분할까? 그때 정리한 **두 조건**을 확인해보면 된다.

| 조건 | `wait/notify` 버전 | Condition 분리 버전 |
| --- | --- | --- |
| ① 대기자가 **모두 같은 조건**을 기다리는가 | ❌ 생산자·소비자 혼재 | ✅ **Condition마다 균일** |
| ② 신호 하나로 **정확히 하나**만 진행 가능한가 | ✅ | ✅ |
| 결론 | ⚠️ `notifyAll()` 필요 | ✅ **`signal()`로 충분** |

**대기 공간을 나눈 순간 ①이 자동으로 충족된다.** 이게 `signalAll()`을 안 써도 되는 근거다. 실제 `ArrayBlockingQueue`도 같은 이유로 `signal()`을 쓴다.

## 3.4 ⚠️ 헷갈리지 말 것 — Condition은 여러 개, 락은 하나

```text
Condition   : 여러 개  (대기 공간)
Lock        : 딱 하나  (입장권)
```

```java
consumerCond.signal();
// ⚠️ 이 순간 소비자가 실행되는 게 아니다
// ⚠️ 깨어난 소비자도 lock 을 다시 얻어야 한다
// ⚠️ signal() 을 호출한 생산자가 unlock() 할 때까지 기다린다
```

**대기 공간을 나눈 것이지 락을 나눈 게 아니다.** `put()`과 `take()`는 여전히 동시에 실행될 수 없다.

---

# 4. notify와 signal의 차이

## 4.1 깨우는 순서

| | `Object.notify()` | `Condition.signal()` |
| --- | --- | --- |
| 깨우는 순서 | **정의되지 않음** | 일반적으로 **FIFO** |
| 근거 | 명세에 없음 | AQS의 조건 큐가 FIFO |
| 호출 조건 | 모니터 락 보유 | 해당 `Lock` 보유 |

## 4.2 ⚠️ "FIFO로 깨운다"와 "FIFO로 실행된다"는 다르다

```text
signal() 호출
    │
    ▼
① 조건 큐에서 FIFO 로 하나를 꺼낸다        ← 여기는 FIFO ✅
    │
    ▼
② 락 대기 큐로 옮긴다
    │
    ▼
③ 락을 획득해야 실행                        ← 여기는 락의 공정성 설정 ⚠️
```

**비공정 락(기본값)이라면 ③에서 새치기가 일어날 수 있다.** 조건 큐에서 FIFO로 나왔어도, 마침 도착한 다른 스레드가 락을 먼저 가져갈 수 있다. 이전 글의 **"공정 모드에서도 `tryLock()`은 새치기한다"**와 같은 층위의 이야기다.

## 4.3 ⭐ 상태 변화가 다르다

이건 놓치기 쉬운 부분이다.

| | `notify()` | `signal()` |
| --- | --- | --- |
| 대기 중 상태 | `WAITING` | `WAITING` |
| 신호 직후 | ⭐ `WAITING` → **`BLOCKED`** | ⭐ `WAITING` → **`WAITING`** |
| 락 획득 후 | `RUNNABLE` | `RUNNABLE` |

```text
[ synchronized ]                    [ ReentrantLock ]

wait() → 대기 집합 (WAITING)         await() → 조건 큐 (WAITING)
   │                                    │
notify()                             signal()
   │                                    │
   ▼                                    ▼
진입 집합 (BLOCKED)                  락 대기 큐 (⭐ 여전히 WAITING)
   │                                    │
락 획득                               락 획득
   │                                    │
   ▼                                    ▼
RUNNABLE                             RUNNABLE
```

`ReentrantLock`은 락 대기조차 `LockSupport.park()`로 구현되어 있어서 **`BLOCKED`가 아예 나타나지 않는다.** 스레드 덤프를 읽을 때 `synchronized` 기반인지 `Lock` 기반인지 구분하는 단서가 된다.

---

# 5. 스레드 대기 총정리

지금까지 나온 대기 방식을 한 표로 모으면 이렇다.

| | 대기 1 — **락 획득** | 대기 2 — **조건 대기** |
| --- | --- | --- |
| **`synchronized`** | | |
| 관리 장소 | 객체의 **진입 집합**(entry set) | 객체의 **대기 집합**(wait set) |
| 상태 | `BLOCKED` | `WAITING` |
| 들어가는 계기 | `synchronized` 진입 실패 | `wait()` 호출 |
| 나오는 계기 | 다른 스레드가 블록을 벗어남 | `notify()` / `notifyAll()` |
| 인터럽트 | ❌ **반응 안 함** | ✅ 반응 |
| **`ReentrantLock`** | | |
| 관리 장소 | `ReentrantLock`의 **대기 큐** | `Condition`의 **대기 공간** |
| 상태 | ⭐ `WAITING` | `WAITING` |
| 들어가는 계기 | `lock.lock()` 실패 | `condition.await()` |
| 나오는 계기 | 다른 스레드가 `unlock()` | `signal()` / `signalAll()` |
| 인터럽트 | ⚠️ **`lock()`은 ❌ / `lockInterruptibly()`는 ✅** | ✅ 반응 |

```text
       synchronized                        ReentrantLock
 ┌─────────────────────────┐        ┌─────────────────────────┐
 │  🔒 모니터 락            │        │  🔒 lock                 │
 │                         │        │                         │
 │  🚪 진입 집합  BLOCKED   │        │  🚪 대기 큐    WAITING   │
 │                         │        │                         │
 │  💤 대기 집합  WAITING   │        │  💤 Condition1 WAITING  │
 │     (딱 1개, 고정)       │        │  💤 Condition2 WAITING  │
 │                         │        │     (원하는 만큼)        │
 └─────────────────────────┘        └─────────────────────────┘
```

---

# 6. lock() vs lockInterruptibly()

## 6.1 lock()이 인터럽트를 안 받는 이유

**이유 ① — `synchronized`와 같은 계약을 제공하기 위해**

`lock()`은 "무슨 일이 있어도 락을 얻는다"는 단순한 계약이다. `synchronized`를 `Lock`으로 갈아끼울 때 동작이 달라지지 않아야 한다.

**이유 ② — 인터럽트를 허용하면 `unlock()` 처리가 까다로워진다**

코드로 보면 명확하다.

```java
// ❌ 흔히 저지르는 실수
try {
    lock.lockInterruptibly();
    // 임계 영역
} catch (InterruptedException e) {
    log("인터럽트");
} finally {
    lock.unlock();      // 💥 락을 못 얻었는데 unlock → IllegalMonitorStateException
}
```

**예외가 던져졌다는 건 락을 획득하지 못했다는 뜻**이므로, `unlock()`을 호출하면 안 된다.

```java
// ✅ 올바른 형태 — lock 획득을 try 밖으로
public void doWork() throws InterruptedException {
    lock.lockInterruptibly();          // 실패하면 여기서 예외 → try 진입 안 함
    try {
        // 임계 영역
    } finally {
        lock.unlock();                 // ✅ 반드시 락을 쥔 상태
    }
}
```

이전 글에서 정리한 **"`lock()`은 `try` 블록 밖에 있어야 한다"**와 같은 원칙이다.

## 6.2 정리

| | `lock()` | `lockInterruptibly()` |
| --- | --- | --- |
| 인터럽트 | ❌ 무시 (플래그는 복원) | ✅ `InterruptedException` |
| 예외 | 없음 | `throws InterruptedException` |
| 획득 실패 시 | 없음 (반드시 획득) | **락을 안 가진 상태로 탈출** |

---

# 7. BlockingQueue — 이미 만들어져 있다

## 7.1 사용

```java
public class BoundedQueueV6_1 implements BoundedQueue {

    private final BlockingQueue<String> queue;

    public BoundedQueueV6_1(int max) {
        this.queue = new ArrayBlockingQueue<>(max);
    }

    @Override
    public void put(String data) {
        try {
            queue.put(data);              // 가득 차면 대기
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }
    }

    @Override
    public String take() {
        try {
            return queue.take();          // 비어 있으면 대기
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }
    }
}
```

**스레드 관점에서 보면 큐의 특정 조건을 만족할 때까지 작업을 차단(blocking)하는 자료구조**다.

- **데이터 추가 차단**: 큐가 가득 차면 `put()`을 시도하는 스레드는 공간이 생길 때까지 차단
- **데이터 획득 차단**: 큐가 비어 있으면 `take()`를 시도하는 스레드는 데이터가 들어올 때까지 차단

## 7.2 ⭐ 내부 구현이 우리가 만든 것과 똑같다

`ArrayBlockingQueue`의 실제 소스다.

```java
public class ArrayBlockingQueue<E> {
    final ReentrantLock lock;
    private final Condition notEmpty;    // ⭐ consumerCond 와 같은 역할
    private final Condition notFull;     // ⭐ producerCond 와 같은 역할

    public void put(E e) throws InterruptedException {
        Objects.requireNonNull(e);
        lock.lockInterruptibly();        // ⭐ lock() 이 아니다
        try {
            while (count == items.length)
                notFull.await();
            enqueue(e);                  // enqueue 안에서 notEmpty.signal()
        } finally {
            lock.unlock();
        }
    }
}
```

| | 직접 만든 V5 | `ArrayBlockingQueue` |
| --- | --- | --- |
| 락 | `ReentrantLock` | `ReentrantLock` |
| Condition | `producerCond` / `consumerCond` | `notFull` / `notEmpty` |
| 락 획득 | `lock()` | ⭐ `lockInterruptibly()` |
| 구조 | 동일 | 동일 |

차이는 `lock()` 대신 `lockInterruptibly()`를 쓴다는 것뿐이다. `put()`이 `throws InterruptedException`을 선언하므로 **락 획득 단계부터 취소 가능**하게 만든 것이다.

> 💡 직접 만들어본 것과 표준 라이브러리가 같은 구조라는 걸 확인하는 게 이 단계의 목적이다.

---

# 8. 4가지 선택지

## 8.1 ⭐ 외워야 할 표

큐가 가득 찼을 때 무엇을 할 것인가? 선택지가 넷이고, 메서드도 넷이다.

| 동작 | **예외 발생** | **특수값 반환** | **블로킹** | **타임아웃** |
| --- | --- | --- | --- | --- |
| **삽입** | `add(e)` | `offer(e)` | ⭐ `put(e)` | `offer(e, time, unit)` |
| **제거** | `remove()` | `poll()` | ⭐ `take()` | `poll(time, unit)` |
| **검사** | `element()` | `peek()` | — | — |

```text
  ① 예외를 던진다        → add(e)        IllegalStateException
  ② 즉시 실패를 알린다   → offer(e)      false 반환
  ③ 기다린다             → put(e)        ⭐ 생산자-소비자의 기본
  ④ 잠깐만 기다린다      → offer(e,t,u)  시간 초과 시 false
```

> 💡 `remove()`와 `remove(Object)`는 다르다. 전자는 **머리 요소**를 제거하고, 후자는 **특정 요소**를 찾아 제거한다.

> 💡 표에 없는 유용한 메서드로 `drainTo(Collection)`이 있다. 큐의 요소를 **한 번에 전부** 옮겨서 배치 처리할 때 쓴다.

## 8.2 타임아웃 동작 확인

```java
queue.offer(data, 1, TimeUnit.NANOSECONDS);   // 사실상 즉시 포기
queue.poll(2000, TimeUnit.MILLISECONDS);      // 2초 대기
```

```text
21:01:06.136 [producer3] 저장 시도 결과: false      ← offer 실패
21:01:06.137 [producer3] [생산 완료] data3 -> [data1, data2]   ← ⚠️ 로그가 오해를 부름

21:01:06.576 [     main] consumer3: TIMED_WAITING   ← poll(2000ms) 대기 중
21:01:08.472 [consumer3] [소비 완료] null <- []     ← 2초 뒤 null
```

> ⚠️ `저장 시도 결과: false`인데 `[생산 완료]`가 찍힌다. `ProducerTask`가 결과와 무관하게 로그를 남기기 때문이다. 실제 코드라면 **반환값을 확인해서 분기**해야 한다.

## 8.3 예외 동작 확인

```java
queue.add(data);      // 가득 차면 IllegalStateException
queue.remove();       // 비어 있으면 NoSuchElementException
```

```text
Exception in thread "producer3" java.lang.IllegalStateException: Queue full
    at java.util.AbstractQueue.add(...)
...
21:03:57.099 [     main] producer3: TERMINATED      ← ⭐ 이 스레드만 죽었다
```

> ⭐ **여기서 주목할 것**: 예외가 터졌는데도 **프로그램이 계속 돌아간다.** producer3만 `TERMINATED`가 되고 나머지는 멀쩡하다. 앞 글에서 정리한 **"`run()`에서 언체크 예외가 나면 그 스레드만 죽고, main은 잡을 수 없다"**가 실증된 장면이다.

---

# 9. 구현체 선택과 실무 주의점

## 9.1 구현체 비교

| 구현체 | 크기 | 특징 |
| --- | --- | --- |
| `ArrayBlockingQueue` | **고정** (필수) | 배열 기반. 락 1개 + Condition 2개 |
| `LinkedBlockingQueue` | 선택 (기본 **무한**) | ⭐ **락 2개**(put/take)라 처리량이 높다 |
| `SynchronousQueue` | **0** | 버퍼가 없다. 생산자와 소비자가 직접 만나야 진행 |
| `PriorityBlockingQueue` | 무한 | 우선순위 순서로 꺼냄 |
| `DelayQueue` | 무한 | 지연 시간이 지난 요소만 꺼낼 수 있음 |
| `LinkedBlockingDeque` | 선택 | 양쪽 끝에서 입출력 |

**`LinkedBlockingQueue`가 더 빠른 이유**

```text
[ ArrayBlockingQueue ]              [ LinkedBlockingQueue ]

    🔒 lock (1개)                       🔒 putLock      🔒 takeLock
        │                                   │               │
   put ─┴─ take                          put              take
   ⚠️ 서로 막는다                       ✅ 동시에 진행 가능
```

배열은 머리와 꼬리가 같은 공간을 공유하므로 락을 나눌 수 없지만, 연결 리스트는 머리와 꼬리가 분리되어 있어 가능하다.

## 9.2 ⚠️ LinkedBlockingQueue의 기본 크기는 무한이다

```java
new LinkedBlockingQueue<>();          // ⚠️ 크기 = Integer.MAX_VALUE
new LinkedBlockingQueue<>(1000);      // ✅ 명시하자
```

**사실상 무한 큐**가 되어, 소비가 느리면 **메모리가 계속 쌓이다가 OOM**이 난다. 그리고 `put()`이 절대 블로킹되지 않으므로 **백프레셔(backpressure)가 사라진다.**

> ⭐ **한정된 버퍼의 진짜 가치는 "생산자를 자연스럽게 늦추는 것"이다.**
> 큐가 가득 차면 `put()`이 막히고, 그 자체가 흐름 제어가 된다. 무한 큐를 쓰면 이 안전장치를 스스로 없애는 셈이다.

## 9.3 ⚠️ BlockingQueue는 null을 허용하지 않는다

```java
queue.put(null);     // 💥 NullPointerException
```

`poll()`이 **null을 "비어 있음"의 신호로 쓰기 때문**이다. null을 허용하면 "값이 null인지" "큐가 비었는지" 구분할 수 없다. 앞 글들에서 반복해서 나온 **"반환값 하나로 두 가지 의미를 담을 수 없다"**는 문제와 같은 맥락이다.

## 9.4 메서드 선택 가이드

```text
데이터를 넣어야 한다
   │
   ├─ 넣을 때까지 기다려도 되나?        → ⭐ put(e)        (기본)
   ├─ 못 넣으면 다른 일을 해야 하나?     → offer(e)
   ├─ 잠깐만 기다려볼까?                → offer(e, t, u)
   └─ 못 넣는 건 버그인가?              → add(e)          (예외)

데이터를 꺼내야 한다
   │
   ├─ 올 때까지 기다려도 되나?          → ⭐ take()        (기본)
   ├─ 없으면 다른 일을 해야 하나?        → poll()
   ├─ 잠깐만 기다려볼까?                → poll(t, u)
   └─ 없는 건 버그인가?                 → remove()        (예외)
```

---

# 10. 전체 여정 정리

## 10.1 생산자-소비자 문제 해결 과정

| 단계 | 방법 | 결과 |
| --- | --- | --- |
| V1 | 버리고 `null` 반환 | ❌ 데이터 유실 |
| V2 | `synchronized` + `sleep()` | 💀 **락을 쥔 채 자서 전체 정지** |
| V3 | `synchronized` + `wait/notify` | ✅ 동작. ⚠️ 헛깨움 |
| V4 | `ReentrantLock` + `Condition` 1개 | ✅ 도구만 교체 |
| V5 | `ReentrantLock` + `Condition` **2개** | ⭐ 헛깨움 해결 |
| V6 | `BlockingQueue` | ⭐ **이미 만들어져 있음** |

## 10.2 핵심 통찰 세 가지

**① 락을 쥔 채 기다리지 마라**

```text
sleep()  → 락 유지  💀
wait()   → 락 반납  ✅
await()  → 락 반납  ✅
```

**② 대기 공간을 목적별로 나눠라**

```text
대기자가 섞여 있으면  → notifyAll() 로 전부 깨워야 안전
대기 공간을 나누면    → signal() 하나로 정확히 필요한 쪽만
```

**③ 조건 확인은 항상 `while`로**

```text
재경쟁으로 조건이 바뀔 수 있고 (이유 ①)
아무 이유 없이 깨어날 수도 있다 (이유 ② — spurious wakeup)
```

## 10.3 동시성 도구 지도

```text
                      공유 상태가 있다
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
    가시성만 필요        원자성 필요         협력·조율 필요
        │                   │                   │
    volatile          synchronized          wait / notify
                      AtomicLong            Condition
                      ReentrantLock         BlockingQueue ⭐
```

실무에서는 대부분 **오른쪽 끝**에서 끝난다. `BlockingQueue` 하나면 생산자-소비자는 해결된다. 왼쪽을 배우는 건 **그 안에서 무슨 일이 벌어지는지 알기 위해서**다.

---

# 마무리 정리

## 한 줄 요약

> **`Condition`은 여러 개, 락은 하나다.**
> 대기 공간을 나누면 `signal()` 하나로 정확히 필요한 쪽만 깨울 수 있고, 그렇게 만든 것이 `BlockingQueue`다.

## 헷갈리기 쉬운 포인트 10가지

| # | 흔한 오해 | 정확한 사실 |
| --- | --- | --- |
| 1 | `Condition`에도 `wait()`가 있다 | **`await()`**다 |
| 2 | Condition을 쓰면 락도 나뉜다 | **락은 하나.** 대기 공간만 나뉜다 |
| 3 | `signal()`을 받으면 바로 실행된다 | **락을 다시 얻어야** 한다 |
| 4 | `signal()`은 `notify()`처럼 `BLOCKED`로 만든다 | **`WAITING` 그대로**다 |
| 5 | `signal()`은 FIFO라 순서가 보장된다 | 조건 큐만 FIFO. **락 획득은 공정성 설정을 따른다** |
| 6 | `lockInterruptibly()`도 `finally`에서 `unlock()` | 예외 시엔 **락이 없어서 `IllegalMonitorStateException`** |
| 7 | `LinkedBlockingQueue`는 크기를 안 주면 작다 | **`Integer.MAX_VALUE`** — 사실상 무한 |
| 8 | 무한 큐가 더 안전하다 | **백프레셔가 사라져** OOM 위험 |
| 9 | `BlockingQueue`에 null을 넣을 수 있다 | **`NullPointerException`** |
| 10 | `add()`와 `offer()`는 같다 | 예외 vs `false`. **실패를 어떻게 알릴지**가 다르다 |

## 대기 방식 최종 정리

| 대기 방법 | 상태 | 락 보유 | 깨어나는 법 | 인터럽트 |
| --- | --- | --- | --- | --- |
| `synchronized` 진입 실패 | `BLOCKED` | ❌ | 락이 풀리면 자동 | ❌ |
| `sleep(ms)` | `TIMED_WAITING` | ⚠️ **유지** | 시간 만료 | ✅ |
| `wait()` | `WAITING` | ✅ 반납 | `notify()` | ✅ |
| `lock.lock()` 실패 | `WAITING` | ❌ | `unlock()` | ❌ |
| `lockInterruptibly()` 실패 | `WAITING` | ❌ | `unlock()` | ✅ |
| `condition.await()` | `WAITING` | ✅ 반납 | `signal()` | ✅ |
| `BlockingQueue.put/take` | `WAITING` | ✅ 반납 | 공간·데이터 생김 | ✅ |

다음 글에서는 스레드 풀과 `ExecutorService`를 다룬다. `BlockingQueue`가 **작업 큐**로 쓰이는 곳이다.