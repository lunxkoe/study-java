# 고급 동기화 - concurrentLock

## LockSupport - 1

### synchronized 단점
- 무한 대기: BLOCKED 상태의 스레드는 락이 풀릴 때까지 무한 대기
  - 특정 시간까지만 대기하는 타임 아웃 X
  - 중간에 인터럽트 X

- 공정성: 락이 들어왔을 때 BLOCKED 상태의 여러 스레드 중 어떤 스레드가 락을 획득할 지 알 수 없음
  - 최악의 경우 특정 스레드가 너무 오랜기간 락을 획득하지 못할 수 있음

- 결론: 더 유연하고, 더 세밀한 제어가 가능한 방법들이 필요하게 됨
    -  java.util.concurrent

### LockSupport 기능
- 스레드를 WAITING 상태로 변경
  - park(): 스레드를 WAITING 상태로 변경함
    - 스레드를 대기 상태로 둠 (누군가 깨워주면 다시 Runnable 상태로 돌아갈 수 있음 - Blocked와의 차이점)
  - parkNanos(nanos): 스레드를 나노초 동안만 TIMED_WAITING 상태로 변경
    - 지정한 나노초가 지나면 다시 Runnable 상태로 돌아옴
  - unpark(thread): Waiting 상태의 대상 스레드를 Runnable 상태로 변경

### 예제 코드
```java
public class LockSupportMainV1 {

    public static void main(String[] args) {
        Thread thread1 = new Thread(new ParkTest(), "Thread-1");
        thread1.start();

        // 잠시 대기하며 Thread-1의 park 상태에 빠질 시간을 줌
        sleep(100);
        log("Thread-1 state: " + thread1.getState());

        log("main -> unpark(Thread-1)");
        LockSupport.unpark(thread1);
        sleep(100);
        log("Thread-1 state: " + thread1.getState());
    }

    static class ParkTest implements Runnable {

        @Override
        public void run() {
            log("park 시작");
            LockSupport.park(); // 해당 스레드를 Waiting 상태로 변경
            log("park 종료, state: " + Thread.currentThread().getState()); // Runnable
            log("인터럽트 상태: " + Thread.currentThread().isInterrupted());
        }
    }
}
```
- 실행 결과
    ```text
    20:41:33.376 [ Thread-1] park 시작
    20:41:33.466 [     main] Thread-1 state: WAITING
    20:41:33.466 [     main] main -> unpark(Thread-1)
    20:41:33.467 [ Thread-1] park 종료, state: RUNNABLE
    20:41:33.468 [ Thread-1] 인터럽트 상태: false
    20:41:33.577 [     main] Thread-1 state: TERMINATED
    ```
    - LockSupport는 특정 스레드를 Waiting 상태로 변경할 수 있음 / 또한 다시 Runnable 상태로 되돌리 수 있음

### 왜 LockSupport.unpark(thread)는 특정 스레드를 지정하는 매개변수가 있을까?
- LockSupport.park()를 호출하여 스스로 대기 상태에 빠질 수 있지만, 대기 상태의 스레드는 자신의 코드를 실행할 수 없기 때문
- 따라서 외부 스레드의 도움을 받아야함

### 인터럽트 사용
- Waiting 상태의 스레드에 인터럽트가 발생하면 Waiting 상태에서 Runnable 상태로 변하면서 깨어남
```java
public class LockSupportMainV1 {

    public static void main(String[] args) {
        Thread thread1 = new Thread(new ParkTest(), "Thread-1");
        thread1.start();

        // 잠시 대기하며 Thread-1의 park 상태에 빠질 시간을 줌
        sleep(100);
        log("Thread-1 state: " + thread1.getState());

        log("main -> unpark(Thread-1)");
//        LockSupport.unpark(thread1);
        thread1.interrupt();
        sleep(100);
        log("Thread-1 state: " + thread1.getState());
    }

    static class ParkTest implements Runnable {

        @Override
        public void run() {
            log("park 시작");
            LockSupport.park(); // 해당 스레드를 Waiting 상태로 변경
            log("park 종료, state: " + Thread.currentThread().getState()); // Runnable
            log("인터럽트 상태: " + Thread.currentThread().isInterrupted());
        }
    }
}
```
- 실행 결과
    ```text
    20:44:57.510 [ Thread-1] park 시작
    20:44:57.590 [     main] Thread-1 state: WAITING
    20:44:57.590 [     main] main -> unpark(Thread-1)
    20:44:57.592 [ Thread-1] park 종료, state: RUNNABLE
    20:44:57.592 [ Thread-1] 인터럽트 상태: true
    20:44:57.701 [     main] Thread-1 state: TERMINATED
    ```
    - 인터럽트 상태가 true가 되어있음

---
## LockSupport - 2

### 시간 대기 - parkNanos(nanos)
- 스레드를 나노초 동안만 Timed_Waiting 상태로 변경
- 지정한 나노초가 지나면 다시 Runnable 상태로 변경됨
- 참고로 밀리초동안 대기하는 스레드는 없음
- parkUntil(millis)라는 메서드가 있긴 한데, 특정 에포크 시간에 맞추어 깨어나는 메서드 => 정확한 미래의 에포크 시점을 지정해야함

### 예제 코드
```java
public class LockSupportMainV2 {

    public static void main(String[] args) {
        Thread thread1 = new Thread(new ParkTest(), "Thread-1");
        thread1.start();

        // 잠시 대기하며 Thread-1의 park 상태에 빠질 시간을 줌
        sleep(100);
        log("Thread-1 state: " + thread1.getState());
    }

    static class ParkTest implements Runnable {

        @Override
        public void run() {
            log("park 시작");
            LockSupport.parkNanos(2000_000000); // 2초
            log("park 종료, state: " + Thread.currentThread().getState()); // Runnable
            log("인터럽트 상태: " + Thread.currentThread().isInterrupted());
        }
    }
}
```
- 실행 결과
    ```text
    20:54:15.214 [ Thread-1] park 시작
    20:54:15.289 [     main] Thread-1 state: TIMED_WAITING
    20:54:17.222 [ Thread-1] park 종료, state: RUNNABLE
    20:54:17.223 [ Thread-1] 인터럽트 상태: false
    ```
    - 나노초: 2000_000000 => 2초
    - 스스르 2초 뒤에 waiting -> runnable 상태로 변경됨

### Blocked vs Waiting
- 인터럽트
  - blocked 상태는 인터럽트가 걸려도 대기 상태를 빠져나오지 못함
    - 인터럽트 상태 true는 됨
  - waiting(timed) 상태는 인터럽트가 걸리면 대기 상태를 빠져나와 runnable 상태로 변함

- 용도
  - blocked 상태는 자바의 synchronized에서 락을 획득하기 위해 대기할 때 사용됨
  - waiting 상태는 스레드가 특정 조건이나 시간동안 대기할때 발생하는 상태
    - Thread.join(), LockSupport.park(), Object.wait()와 같은 메서드 호출 시 waiting 상태가 됨
    - Thread.sleep(ms), Object.wait(long timeout), Thread.join(long millis), LockSupport(ns)

### 활용
```java
if (!lock.tryLock(10초)) { // 내부에서 parkNanos() 사용
    log("[진입 실패] 너무 오래 기다렸습니다.");
    return false;
}

// 임계 영역 시갖

// 임계 영역 종료
lock.unlock(); // 내부에서 unpark() 사용
```
- 근데 락을 직접 만드는 것은 어려움
- LockSupport가 너무 저수준임
- 해결책: ReentrantLock이라는 구현체와 Lock이라는 인터페이스를 제공함

---
## ReentrantLock - 이론

### Lock 인터페이스
```java
public interface Lock {
    void lock();
    void lockInterrputibly();
    boolean tryLock();
    boolean tryLock(long time, TimeUnit unit);
    void unlock();
    Condition newCondition();
}
```
- void lock()
  - 락을 획득함
  - 만약 다른 스레드가 이미 락을 획득했다면 락이 풀릴 때까지 현재 스레드를 `waiting` 상태로 만듦
  - 주의: **인터럽트에 응답하지 않음**
  - 주의: 여기서 사용하는 락은 객체 내부에 있는 모니터 락이 아님
    - Lock 인터페이스와 ReentrantLock이 제공하는 기능임

- void lockInterrputibly()
  - 락 획득을 시도하되, 다른 스레드가 인터럽트 할 수 있도로고 함
  - 만약 다른 스레드가 이미 락을 획득했다면, 현재 스레드는 락을 획득할 때까지 대기함
  - 대기 중 인터럽트가 발생하면 InterruptedException이 발생하고 락 획득을 포기함

- boolean tryLock()
  - 락 획득을 시도하고, 즉시 성공 여부를 반환함
  - 만약 다른 스레드가 락을 획득했다면 false를 반환하고, 그렇지 않으면 락을 획득하고 true를 반환

- boolean tryLock(long time, TimeUnit unit)
  - 주어진 시간 동안 락 획득을 시도함
  - 주어진 시간 안에 획득하면 true, 아니면 false
  - 대기 중 인터럽트가 발생하면 InterruptedException이 발생하고 락 획득을 포기함

- void unlock()
  - 락을 해제함
  - 락을 획득한 스레드가 호출해야하며, 그렇지 않으면 `IllegalMonitorStateException`일 발생할 수 있음

- Condition newCondition()
  - Condition 객체를 생성하여 반환함
  - Condition 객체는 락과 결합되어 사용되며, 스레드가 특정 조건을 기다리거나 신호를 받을 수 있도록 함
  - 이는 Object 클래스의 wait, notify, notifyAll 메서드와 유사한 역할을 함
  - 뒤에서 자세히 다룸

### 참고
- lock() 메서드는 인터럽트에 응하지 않는다고 되어있음
- 의도는 인터럽트가 발생해도 무시하고 락을 기다리는 것
- waiting에서 인터럽트를 받으면 runnable 상태로 변함
  - 응하지 않는다가 상태가 변경된다는 의미가 아닌 아주 짧은 시간동안 runnable 상태로 돌아갔다가 다시 waiting 상태로 강제로 변경해버리는 것
  - 인터럽트가 필요하면 lockInterrputibly 사용하면 됨

### 공정성
- 락이 돌아왔을 대 어떤 스레드가 락을 얻을 수 있을지 알 수 없음
- ReentrantLock: 스레드가 공정하게 락을 얻을 수 있는 모드르 제공함
```java
public class ReentrantLockEx {

    // 비공정 모드
    private final Lock nonFairLock = new ReentrantLock();

    // 공정모드
    private final Lock fairLock = new ReentrantLock(true);

    public void nonFairLockTest() {
        nonFairLock.lock();
        try {
            // 임계 영역
        } finally {
            nonFairLock.unlock();
        }
    }

    public void fairLockTest() {
        fairLock.lock();
        try {
            // 임계 영역
        } finally {
            fairLock.unlock();
        }
    }
}
```

### 비공정 모드
- 기본 모드
- 락을 요청한 스레드가 락을 먼저 획득한다는 보장이 없음
- 락을 풀었을 대, 대기 중인 스레드 중 아무나 락을 획득할 수 있음
- 락을 빨리 획득할 수 있지만, 특정 스레드가 장기간 락을 획득하지 못할 가능성도 있음
- 특징
  - 성능 우선: 락을 획득하는 속도가 빠름
  - 선점 가능: 새로운 스레드가 기존 대기 스레드보다 먼저 락을 획득할 수 있음
  - 기아 현상 가능성: 특정 스레드가 계속해서 락을 획득하지 못할 수 있음

### 공정 모드
- 생성자에서 true를 전달하면 됨
- 락을 요청한 순서대로 스레드가 락을 획득할 수 있게 함
- 특징
  - 공정성 보장: 대기 큐에서 먼저 대기한 스레드가 락을 먼저 획득함
  - 기아 현상 방지: 모든 스레드가 언젠가 락을 획득할 수 있게 보장됨
  - 성능 저하: 락을 획득하는 속도가 느려질 수 있음

---
## ReentrantLock - 활용

### 예제 코드
```java
public class BankAccountV4 implements BankAccount {

    private final Lock lock = new ReentrantLock();

    private int balance;

    public BankAccountV4(int balance) {
        this.balance = balance;
    }

    @Override
    public boolean withdraw(int amount) {
        log("거래 시작: " + getClass().getSimpleName());

        lock.lock();
        try {
            log("[검증 시작] 출금액: " + amount + ", 잔액: " + balance);
            if (balance < amount) {
                log("[검증 실패] 출금액: " + amount + ", 잔액: " + balance);
                return false;
            }

            log("[검증 완료] 출금액: " + amount + ", 잔액: " + balance);
            sleep(1000); // 출금에 걸리는 시간
            balance -= amount;
            log("[출금 완료] 출금액: " + amount + ", 잔액: " + balance);
        } finally {
            lock.unlock();
        }

        log("거래 종료: " + getClass().getSimpleName());
        return true;
    }

    @Override
    public synchronized int getBalance() {
        return balance;
    }
}
```
- 예외가 발생하더라도 반드시 unlock을 해주어야함!!!! - `try - finally` 사용
- 실행 결과
    ```text
    21:38:52.807 [       t1] 거래 시작: BankAccountV4
    21:38:52.807 [       t2] 거래 시작: BankAccountV4
    21:38:52.823 [       t1] [검증 시작] 출금액: 800, 잔액: 1000
    21:38:52.824 [       t1] [검증 완료] 출금액: 800, 잔액: 1000
    21:38:53.292 [     main] t1 state: TIMED_WAITING
    21:38:53.294 [     main] t2 state: WAITING
    21:38:53.839 [       t1] [출금 완료] 출금액: 800, 잔액: 200
    21:38:53.839 [       t2] [검증 시작] 출금액: 800, 잔액: 200
    21:38:53.839 [       t1] 거래 종료: BankAccountV4
    21:38:53.840 [       t2] [검증 실패] 출금액: 800, 잔액: 200
    21:38:53.841 [     main] 최종 잔액: 200
    ```
    - 이제 Blocked 상태가 아닌 waiting 상태로 변경됨

### 주의
- 여기서 사용하는 락은 객체 내부에 있는 모니터 락이 아님
- Lock 인터페이스와 ReentrantLock이 제공하는 기능

### 동작 원리
- ReentrantLock 내부에 lock이랑 대기 큐가 있음
- t1이 락을 사용하는 동안
- t2는 대기 큐에서 Waiting 상태로 대기 하고 있음
- t1이 unlock을 호출하면 대기 큐에 있던 스레드를 waiting -> runnable로 변경해줌
  - 비공정 모드: 아무나 깨움
  - 공정 모드: 큐에 들어온 순서대로 깨움

---
## ReentrantLock - 대기 중단

### 메서드
- boolean tryLock()
  - 즉시 성공 여부 반환
- boolean tryLock(long time, TimeUnit unit)
  - 특정 시간만큼 기다림
  - 인터럽트 영향으로 깨어날 수 있음

### 예제 코드
```java
public class BankAccountV5 implements BankAccount {

    private final Lock lock = new ReentrantLock();

    private int balance;

    public BankAccountV5(int balance) {
        this.balance = balance;
    }

    @Override
    public boolean withdraw(int amount) {
        log("거래 시작: " + getClass().getSimpleName());

        if (!lock.tryLock()) {
            log("[진입 실패]: 이미 처리 중인 작업이 있습니다.");
            return false;
        }

        try {
            log("[검증 시작] 출금액: " + amount + ", 잔액: " + balance);
            if (balance < amount) {
                log("[검증 실패] 출금액: " + amount + ", 잔액: " + balance);
                return false;
            }

            log("[검증 완료] 출금액: " + amount + ", 잔액: " + balance);
            sleep(1000); // 출금에 걸리는 시간
            balance -= amount;
            log("[출금 완료] 출금액: " + amount + ", 잔액: " + balance);
        } finally {
            lock.unlock();
        }

        log("거래 종료: " + getClass().getSimpleName());
        return true;
    }

    @Override
    public int getBalance() {
        lock.lock();
        try {
            return balance;
        } finally {
            lock.unlock();
        }
    }
}
```

```java
public class BankAccountV6 implements BankAccount {

    private final Lock lock = new ReentrantLock();

    private int balance;

    public BankAccountV6(int balance) {
        this.balance = balance;
    }

    @Override
    public boolean withdraw(int amount) {
        log("거래 시작: " + getClass().getSimpleName());

        try {
            if (!lock.tryLock(1000, TimeUnit.MILLISECONDS)) {
                log("[진입 실패]: 이미 처리 중인 작업이 있습니다.");
                return false;
            }
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }

        try {
            log("[검증 시작] 출금액: " + amount + ", 잔액: " + balance);
            if (balance < amount) {
                log("[검증 실패] 출금액: " + amount + ", 잔액: " + balance);
                return false;
            }

            log("[검증 완료] 출금액: " + amount + ", 잔액: " + balance);
            sleep(1000); // 출금에 걸리는 시간
            balance -= amount;
            log("[출금 완료] 출금액: " + amount + ", 잔액: " + balance);
        } finally {
            lock.unlock();
        }

        log("거래 종료: " + getClass().getSimpleName());
        return true;
    }

    @Override
    public int getBalance() {
        lock.lock();
        try {
            return balance;
        } finally {
            lock.unlock();
        }
    }
}
```
