# 동기화 — synchronized

> **표기 규칙**
> - 🔴 **정정** — 원문에 사실과 다르거나 오해를 부르는 내용
> - 🟡 **보강** — 틀리진 않지만 엄밀하지 않아 조건을 붙인 부분
> - 💡 **추가** — 지금 단계에서 함께 알아두면 좋은 내용

---

## 목차

1. [공유 자원과 동시성 문제](#1-공유-자원과-동시성-문제)
2. [문제 분석 — 무엇이 깨진 걸까](#2-문제-분석--무엇이-깨진-걸까)
3. [임계 영역](#3-임계-영역)
4. [synchronized 메서드](#4-synchronized-메서드)
5. [모니터 락 깊게 보기 💡](#5-모니터-락-깊게-보기-)
6. [synchronized 코드 블록](#6-synchronized-코드-블록)
7. [락 객체를 무엇으로 할 것인가 💡](#7-락-객체를-무엇으로-할-것인가-)
8. [⭐ 임계 영역을 진짜로 줄이기](#8--임계-영역을-진짜로-줄이기)
9. [synchronized의 한계와 다음 단계 💡](#9-synchronized의-한계와-다음-단계-)
10. [원문 점검 결과 요약](#10-원문-점검-결과-요약)

---

## 1. 공유 자원과 동시성 문제

### 1.1 용어

| 용어 | 정의 |
| --- | --- |
| **공유 자원** | 여러 스레드가 함께 접근하는 자원 (여기서는 `balance`) |
| **동시성 문제** | 여러 스레드가 동시에 접근할 때 생기는 예상 밖의 결과 |
| **임계 영역** | 한 번에 하나의 스레드만 실행해야 하는 코드 구간 |

🔴 **오타 정정**: "멀티 스레드를 사용하 때는" → **사용할 때는**, "동시서 문제" → **동시성 문제**

### 1.2 문제의 코드

```java
public class BankAccountV1 implements BankAccount {

    private int balance;

    public BankAccountV1(int balance) {
        this.balance = balance;
    }

    @Override
    public boolean withdraw(int amount) {
        log("거래 시작: " + getClass().getSimpleName());

        log("[검증 시작] 출금액: " + amount + ", 잔액: " + balance);
        if (balance < amount) {                    // ① 검증 (check)
            log("[검증 실패] 출금액: " + amount + ", 잔액: " + balance);
            return false;
        }

        log("[검증 완료] 출금액: " + amount + ", 잔액: " + balance);
        sleep(1000);                                // 출금에 걸리는 시간
        balance -= amount;                          // ② 출금 (act)
        log("[출금 완료] 출금액: " + amount + ", 잔액: " + balance);

        log("거래 종료: " + getClass().getSimpleName());
        return true;
    }

    @Override
    public int getBalance() {
        return balance;
    }
}
```

### 1.3 단일 스레드에서는 멀쩡하다

```text
18:02:24.450 [       t1] 거래 시작: BankAccountV1
18:02:24.460 [       t1] [검증 시작] 출금액: 800, 잔액: 1000
18:02:24.461 [       t1] [검증 완료] 출금액: 800, 잔액: 1000
18:02:25.471 [       t1] [출금 완료] 출금액: 800, 잔액: 200
18:02:25.472 [     main] 최종 잔액: 200               ← ✅ 정상
```

### 1.4 두 스레드가 동시에 출금하면

```java
Thread t1 = new Thread(new WithdrawTask(bankAccount, 800), "t1");
Thread t2 = new Thread(new WithdrawTask(bankAccount, 800), "t2");

t1.start();
t2.start();

sleep(500);   // 검증 완료까지 잠시 대기
log("t1 state: " + t1.getState());
log("t2 state: " + t2.getState());

t1.join();
t2.join();
log("최종 잔액: " + bankAccount.getBalance());
```

```text
18:05:18.729 [       t2] [검증 시작] 출금액: 800, 잔액: 1000
18:05:18.729 [       t1] [검증 시작] 출금액: 800, 잔액: 1000
18:05:18.730 [       t2] [검증 완료] 출금액: 800, 잔액: 1000   ← 둘 다 통과
18:05:18.730 [       t1] [검증 완료] 출금액: 800, 잔액: 1000
18:05:19.202 [     main] t1 state: TIMED_WAITING
18:05:19.205 [     main] t2 state: TIMED_WAITING
18:05:19.733 [       t1] [출금 완료] 출금액: 800, 잔액: -600
18:05:19.733 [       t2] [출금 완료] 출금액: 800, 잔액: 200
18:05:19.736 [     main] 최종 잔액: -600              ← 💥 잔액 1000인데 1600 출금
```

**검증 로직이 분명히 있는데도 음수 잔액이 나왔다.**

---

## 2. 문제 분석 — 무엇이 깨진 걸까

### 2.1 타임라인

```text
시간 ──────────────────────────────────────────────────▶
        0ms              500ms             1000ms

t1   │검증: 1000 ≥ 800 ✅│←── sleep(1000) ──→│ balance -= 800
                                              │   (읽은 값 1000)
t2   │검증: 1000 ≥ 800 ✅│←── sleep(1000) ──→│ balance -= 800
     ▲                                        │
     │                                        ▼
  둘 다 같은 1000을 보고 통과              최종 -600
```

### 2.2 ⭐ 이건 "갱신 손실"이 아니라 "check-then-act" 문제다

🟡 **원문은 두 가지 시나리오를 섞어서 설명하는데, 실제로 터진 건 앞쪽이다.** 구분해두면 훨씬 명확하다.

| | check-then-act | lost update (갱신 손실) |
| --- | --- | --- |
| 깨지는 지점 | **검증과 실행 사이**의 시간 틈 | `balance -= amount` **연산 자체** |
| 이 예제의 결과 | **-600** (둘 다 성공) | 200 (한 번만 반영) |
| 원인 | 검증 시점의 값이 실행 시점엔 낡음 | 읽기→계산→쓰기가 원자적이지 않음 |
| `volatile`로 해결? | ❌ **불가능** | ❌ **불가능** |
| `AtomicInteger`로? | ❌ 단순 연산으로는 불가 | ✅ `addAndGet()` |

이 예제에서 최종 잔액이 **200이 아니라 -600**이라는 게 중요한 단서다. 뺄셈이 두 번 다 반영됐으니 **갱신은 손실되지 않았다.** 문제는 **1000원을 보고 통과한 검증이 실행 시점에는 이미 유효하지 않았다**는 것이다.

> ⭐ **`sleep(1000)`은 check와 act 사이의 틈을 1초로 벌려서 문제를 100% 재현하게 만든 장치다.**
> 실제 코드에서는 이 틈이 나노초 단위라 **가끔만 터진다.** 그래서 더 위험하다.

### 2.3 그런데 `balance -= amount`도 원자적이지 않다

원문이 설명한 두 번째 시나리오다.

```java
balance -= amount;

// 실제로는 3단계
// ① balance 를 읽는다        (read)
// ② amount 를 뺀다           (modify)
// ③ balance 에 저장한다       (write)
```

앞 글의 `count++`와 정확히 같은 구조다. 이 예제에서는 `sleep(1000)` 덕분에 두 스레드의 뺄셈이 시간적으로 벌어져 우연히 드러나지 않았을 뿐, **이것도 독립적인 버그다.**

🔴 **원문 오타 두 개는 꽤 위험하다**
- `balance =- amount` → **`balance -= amount`**
  `=-`는 오타가 아니라 **컴파일되는 다른 코드**다. `balance = -amount`가 되어 잔액이 `-800`이 된다.
- "같은 balance가 둘 다 10000원으로 일고" → **1000원으로 읽고**

### 2.4 🟡 "volatile 때문 아닐까? → 아님"이 왜 맞는지

원문의 결론은 정확하다. 근거를 보태면 이렇다.

```java
private volatile int balance;   // 붙여도 -600 이 그대로 나온다
```

| `volatile`이 보장하는 것 | 이 문제에 도움이 되나 |
| --- | --- |
| 가시성 — 최신 값을 읽는다 | 🟡 값은 최신으로 읽지만 |
| 순서 — 재정렬 방지 | 🟡 순서는 문제가 아니었고 |
| **원자성 — 보장 안 함** | ❌ **여기가 깨진 지점** |

t2가 검증할 때 `balance`는 **진짜로 1000이었다.** 최신 값을 정확히 읽었는데도 문제가 생긴 것이다. 가시성이 아니라 **"검증부터 실행까지를 한 덩어리로 묶는 것"**이 필요하다.

💡 **`volatile`은 "값을 언제 보느냐"의 문제를 풀고, `synchronized`는 "누가 언제 들어오느냐"의 문제를 푼다.** 층위가 다르다.

---

## 3. 임계 영역

### 3.1 정의

> **임계 영역(Critical Section)**
> 여러 스레드가 동시에 접근하면 데이터 불일치나 예상치 못한 동작이 발생할 수 있는 코드 구간.
> **한 번에 하나의 스레드만 접근하도록 보호해야 한다.**

### 3.2 근본 원인

```text
공유 자원(balance)을  →  여러 단계(검증 / 출금)로 나누어 사용하기 때문
                              │
                              ▼
                     그 사이에 다른 스레드가 끼어들 수 있다
```

**단계가 하나뿐이었다면 문제가 없었다.** 단계를 나누는 순간 "그 사이"라는 틈이 생긴다.

### 3.3 💡 함께 알아두면 좋은 용어

| 용어 | 의미 |
| --- | --- |
| **경쟁 상태 (Race Condition)** | 실행 타이밍에 따라 결과가 달라지는 상태 |
| **상호 배제 (Mutual Exclusion)** | 한 번에 하나만 들어가게 하는 것. `synchronized`가 제공 |
| **원자성 (Atomicity)** | 여러 단계가 쪼개지지 않는 하나의 동작처럼 보이는 성질 |

`synchronized`는 **상호 배제를 통해 원자성을 만들어내는 도구**다.

---

## 4. synchronized 메서드

### 4.1 적용

```java
@Override
public synchronized boolean withdraw(int amount) {
    // ... 내용 동일
}
```

```text
19:00:15.125 [       t1] [검증 시작] 출금액: 800, 잔액: 1000
19:00:15.126 [       t1] [검증 완료] 출금액: 800, 잔액: 1000
19:00:15.583 [     main] t1 state: TIMED_WAITING     ← sleep 중
19:00:15.584 [     main] t2 state: BLOCKED           ← 락 대기 중
19:00:16.127 [       t1] [출금 완료] 출금액: 800, 잔액: 200
19:00:16.128 [       t2] 거래 시작: BankAccountV2    ← 이제야 진입
19:00:16.128 [       t2] [검증 시작] 출금액: 800, 잔액: 200
19:00:16.129 [       t2] [검증 실패] 출금액: 800, 잔액: 200   ← ✅ 정상
19:00:16.129 [     main] 최종 잔액: 200
```

### 4.2 동작

```text
        BankAccountV2 인스턴스 @x001
        ┌──────────────────────────┐
        │  balance = 1000          │
        │  🔒 모니터 락 (모든 객체가 가짐) │
        └──────────────────────────┘
                   ▲
        ┌──────────┴──────────┐
        │                     │
   t1: 락 획득 ✅         t2: 락 획득 실패 ❌
        │                     │
   withdraw() 진입        BLOCKED 로 대기
        │                     │  (CPU 스케줄링에서 제외)
   sleep(1000)                │
        │                     │
   balance -= 800             │
        │                     │
   메서드 종료 → 락 반납 ──────▶ 락 획득 ✅
                              │
                         withdraw() 진입
                         검증 실패 → false
```

### 4.3 ⭐ 로그에서 읽어낼 것 — `t1: TIMED_WAITING`, `t2: BLOCKED`

앞 글들에서 다룬 두 상태가 한 화면에 잡혔다.

| 스레드 | 상태 | 이유 |
| --- | --- | --- |
| t1 | `TIMED_WAITING` | `sleep(1000)` 중 |
| t2 | `BLOCKED` | 모니터 락 대기 중 |

> ⭐ **여기가 핵심이다: t1은 자고 있는데도 락을 쥐고 있다.**
> 앞 글에서 정리한 **"`sleep()`은 락을 반납하지 않는다"**가 그대로 드러난 장면이다. (`wait()`였다면 반납했을 것이다.)
> 그래서 t2는 **t1이 자는 1초 동안 아무것도 못 하고 통째로 기다린다.** 이 사실이 8장의 논의로 이어진다.

### 4.4 참고 사항 보강

🔴 **문장 정리**: "락을 획득하는지는 자바 표준에 정의되어있지 않음" → **"락을 획득하는 순서는 자바 표준에 정의되어 있지 않음"**

💡 **`synchronized`는 불공정(non-fair) 락이다.**

```text
대기 큐: [t2(10초 대기)] [t3(5초 대기)]
                  ▲
                  │  t4 가 방금 도착
                  │
          ⚠️ t4 가 t2, t3 을 제치고 가져갈 수 있다 (barging)
```

- 오래 기다린 순서대로 주지 않는다
- 이론적으로 **기아(starvation)** — 특정 스레드가 영원히 락을 못 얻는 상황이 가능하다
- 대신 **성능은 공정 락보다 좋다** (컨텍스트 스위칭이 적음)
- 순서가 꼭 필요하면 `new ReentrantLock(true)` (공정 모드). 단, 훨씬 느리다

⭐ **"volatile 없이도 가시성이 해결된다"의 정확한 근거**

앞 글에서 정리한 happens-before의 **모니터 락 규칙**이다.

```text
t1: synchronized 진입
    balance = 200
    synchronized 해제      ──┐
                             │ ⭐ 해제 → 획득 happens-before
                             ▼
t2:                      synchronized 획득
                         balance 읽기 → ✅ 반드시 200 이 보인다
```

🟡 **단, "락을 잡고 접근할 때만" 보장된다.**

```java
public int getBalance() {   // ⚠️ V2 에는 synchronized 가 없다
    return balance;
}
```

`getBalance()`는 락을 잡지 않으므로 **happens-before 관계가 생기지 않는다.** 이 예제에서는 `join()`이 가시성을 만들어줘서 정상 값이 나오지만, 일반적인 상황이라면 낡은 값을 읽을 수 있다.

🔴 **원문은 V3에 와서야 `getBalance()`에 `synchronized`를 붙이는데, 설명 없이 바뀐다.** V2 시점에도 필요한 것이므로 짚고 넘어가는 게 좋다.

---

## 5. 모니터 락 깊게 보기 💡

원문에 없지만 반드시 알아야 할 세 가지다.

### 5.1 락의 대상이 무엇인가 — 인스턴스 vs 클래스

```java
class Counter {
    synchronized void a() { }          // 🔒 this 에 락
    synchronized static void b() { }   // 🔒 Counter.class 에 락
}
```

| 형태 | 락 대상 | 동등한 블록 |
| --- | --- | --- |
| `synchronized` 인스턴스 메서드 | **`this`** | `synchronized (this) { }` |
| `synchronized` static 메서드 | **`클래스 객체`** | `synchronized (Counter.class) { }` |

> ⚠️ **이 둘은 서로 다른 락이다.** `a()`와 `b()`는 **동시에 실행될 수 있다.**
> static 필드를 인스턴스 메서드에서 `synchronized`로 보호하려 했다면, 그건 보호가 아니다.

```java
class Wrong {
    private static int count;                      // static 공유 자원

    synchronized void increment() { count++; }     // ⚠️ this 에 락
    // → 인스턴스가 2개면 락도 2개. count 는 전혀 보호되지 않는다
}
```

### 5.2 ⭐ 재진입 가능(reentrant)하다

```java
class Foo {
    synchronized void outer() {
        inner();          // ✅ 데드락 안 걸림
    }
    synchronized void inner() { }
}
```

같은 스레드가 **이미 가진 락을 다시 획득하는 건 허용된다.** JVM이 락마다 **획득 횟수(카운터)**를 세고, 0이 될 때 완전히 해제한다.

```text
t1: outer() 진입 → 락 획득, 카운터 = 1
    inner() 진입 → 이미 내 락, 카운터 = 2
    inner() 종료 → 카운터 = 1  (아직 해제 안 됨)
    outer() 종료 → 카운터 = 0  ⭐ 여기서 해제
```

재진입이 안 됐다면 **자기가 자기를 기다리는 데드락**이 생겼을 것이다. `ReentrantLock`의 이름도 여기서 왔다.

### 5.3 예외가 나도 락은 자동으로 풀린다

```java
synchronized void risky() {
    balance -= 1000;
    throw new RuntimeException("펑");   // 💥
}   // ⭐ 락은 자동 해제된다
```

컴파일된 바이트코드를 보면 `monitorenter` / `monitorexit`가 **예외 핸들러와 함께** 짝지어져 있다. 그래서 `try-finally`를 직접 쓸 필요가 없다.

> 💡 이게 `ReentrantLock`과의 큰 차이다. `ReentrantLock`은 **반드시 직접 풀어줘야 한다.**
> ```java
> lock.lock();
> try {
>     // ...
> } finally {
>     lock.unlock();   // ⭐ 빠뜨리면 영원히 안 풀림
> }
> ```

🟡 **다만 락이 풀린다고 데이터가 멀쩡한 건 아니다.** 위 예제에서 `balance`는 이미 1000이 빠진 채로 남는다. **락은 일관성을 지켜주지 않는다. 배타성만 지켜준다.**

---

## 6. synchronized 코드 블록

### 6.1 왜 필요한가

`synchronized` 메서드는 **메서드 전체**를 임계 영역으로 만든다. 그런데 정말 보호가 필요한 건 일부뿐인 경우가 많다.

```java
@Override
public boolean withdraw(int amount) {
    log("거래 시작: " + getClass().getSimpleName());    // 공유 자원 안 건드림

    synchronized (this) {                               // ⭐ 여기부터
        log("[검증 시작] ... 잔액: " + balance);
        if (balance < amount) {
            log("[검증 실패] ...");
            return false;
        }
        log("[검증 완료] ...");
        sleep(1000);
        balance -= amount;
        log("[출금 완료] ... 잔액: " + balance);
    }                                                    // ⭐ 여기까지

    log("거래 종료: " + getClass().getSimpleName());     // 공유 자원 안 건드림
    return true;
}

@Override
public synchronized int getBalance() {
    return balance;
}
```

| 문법 | 의미 |
| --- | --- |
| `synchronized (this)` | `this` 인스턴스의 모니터 락을 사용 |
| 블록 밖 | 여러 스레드가 동시 실행 가능 |
| 블록 안 | 한 번에 하나만 |

### 6.2 🟡 블록 안의 `return`은 안전한가

```java
synchronized (this) {
    if (balance < amount) {
        return false;      // ⚠️ 블록 한가운데서 탈출
    }
}
```

**안전하다.** 5.3에서 본 대로 `monitorexit`가 모든 탈출 경로에 걸려 있다. 다만 **검증 실패 시 "거래 종료" 로그가 안 찍힌다**는 점은 V1/V2와 동작이 달라진 부분이다. 로그 일관성을 원하면 `boolean` 변수를 두고 블록 밖에서 처리하는 편이 낫다.

---

## 7. 락 객체를 무엇으로 할 것인가 💡

원문은 `synchronized (this)`만 다루는데, **락 객체 선택은 실무에서 사고가 잦은 지점**이다.

### 7.1 ⚠️ `this`를 락으로 쓰면 외부에 노출된다

```java
// 내 클래스
class BankAccount {
    public boolean withdraw(int amount) {
        synchronized (this) { ... }
    }
}

// 남의 코드
BankAccount account = ...;
synchronized (account) {        // ⚠️ 내 락을 외부에서 잡아버림
    Thread.sleep(100000);       // ⚠️ 내 withdraw() 가 100초 동안 멈춤
}
```

**락 객체가 `public`이면, 그 락은 사실상 공개 API다.** 내가 통제할 수 없는 곳에서 잠글 수 있다.

### 7.2 ✅ 전용 락 객체를 쓰자

```java
public class BankAccountV4 implements BankAccount {

    private int balance;
    private final Object lock = new Object();   // ⭐ private final

    @Override
    public boolean withdraw(int amount) {
        synchronized (lock) {
            // ...
        }
    }
}
```

| | `synchronized (this)` | `private final Object lock` |
| --- | --- | --- |
| 외부에서 잠글 수 있나 | ⚠️ 가능 | ✅ 불가능 |
| 락을 여러 개로 쪼갤 수 있나 | ❌ 하나뿐 | ✅ 용도별로 분리 가능 |
| 코드 간결성 | 간단 | 필드 하나 추가 |

앞 글에서 **"`Thread` 객체를 락으로 쓰지 마라"**고 했던 것과 같은 원칙이다.

### 7.3 ⚠️ 절대 락으로 쓰면 안 되는 것들

```java
private String lock = "LOCK";           // ❌ 문자열 리터럴 — String Pool 공유!
private Integer lock = 0;               // ❌ Integer 캐시 (-128~127) 공유
private Boolean lock = Boolean.FALSE;   // ❌ 싱글턴
```

문자열 리터럴은 **JVM 전체가 하나의 객체를 공유**한다. 전혀 무관한 라이브러리가 같은 문자열로 락을 걸면 서로의 코드가 막힌다. 디버깅이 거의 불가능한 버그가 된다.

```java
private final Object lock = new Object();   // ✅ 항상 이렇게
```

---

## 8. ⭐ 임계 영역을 진짜로 줄이기

### 8.1 🔴 V3는 임계 영역을 거의 줄이지 못했다

원문은 "임계 영역을 작게 유지하자"는 방향을 제시하고 V3로 넘어가는데, **실제로 줄어든 건 로그 두 줄뿐이다.**

```text
[ V2: synchronized 메서드 ]        [ V3: synchronized 블록 ]

┌─ 🔒 락 ──────────────────┐        거래 시작 로그         ← 락 밖 ✅
│ 거래 시작 로그           │        ┌─ 🔒 락 ─────────────┐
│ 검증                     │        │ 검증                │
│ sleep(1000)  ⚠️          │        │ sleep(1000)  ⚠️     │ ← 여전히 안!
│ balance -= amount        │        │ balance -= amount   │
│ 거래 종료 로그           │        └─────────────────────┘
└──────────────────────────┘        거래 종료 로그         ← 락 밖 ✅

  락 보유 시간: 약 1000ms              락 보유 시간: 약 1000ms
                                       (거의 그대로)
```

**`sleep(1000)`이 락 안에 그대로 있다.** 이게 진짜 병목이다. 4.3에서 확인했듯 **자는 동안에도 락은 반납되지 않으므로**, 다른 스레드는 1초를 통째로 기다린다.

### 8.2 왜 이 예제는 sleep을 뺄 수 없나

`sleep(1000)`은 "출금에 걸리는 시간"을 흉내 낸 것이다. 실제로는 **외부 시스템 호출(은행 전산망, DB 커밋)**이라고 보면 된다.

```text
검증 → [외부 시스템 호출 1초] → 잔액 차감
  ▲                              ▲
  └──── 이 전체를 락으로 묶으면 ────┘
        1초 동안 다른 거래가 전부 멈춘다
```

문제는 **check-then-act를 유지하려면 검증과 차감이 같은 락 안에 있어야 한다**는 것이다. 그렇다고 1초짜리 I/O를 락 안에 두면 처리량이 초당 1건으로 떨어진다.

### 8.3 💡 실무의 해법 — 예약 후 확정 패턴

```java
public boolean withdraw(int amount) {
    // ① 짧은 임계 영역: 검증 + 차감을 원자적으로
    synchronized (lock) {
        if (balance < amount) return false;
        balance -= amount;              // ⭐ 먼저 빼둔다 (예약)
    }
    // 락 보유 시간: 마이크로초 단위

    // ② 느린 작업은 락 밖에서
    try {
        externalBankApi.transfer(amount);   // 1초 걸리는 I/O
    } catch (Exception e) {
        synchronized (lock) {
            balance += amount;              // ③ 실패 시 롤백
        }
        return false;
    }
    return true;
}
```

| | 원문 V3 | 예약 후 확정 |
| --- | --- | --- |
| 락 보유 시간 | **약 1000ms** | **마이크로초 단위** |
| 동시 처리 가능 건수 | 초당 1건 | 초당 수천 건 |
| 정합성 | ✅ | ✅ (롤백으로 보정) |
| 복잡도 | 낮음 | 보상 로직 필요 |

> ⭐ **핵심 원칙: 락 안에서는 절대 느린 일을 하지 마라.**
>
> | 락 안에 두면 안 되는 것 | 이유 |
> | --- | --- |
> | `sleep()` | 자면서 락을 쥔다 |
> | 네트워크 I/O, DB 호출 | 수백 ms ~ 수 초 |
> | 파일 읽기/쓰기 | 느림 |
> | 다른 객체의 `synchronized` 호출 | **데드락 위험** |
> | 무거운 연산, 로깅 | 필요 이상으로 길어짐 |

💡 강의 예제에서 `sleep`을 빼지 않는 건 **"락이 잡혀 있다"는 사실을 눈으로 보여주기 위한 장치**다. 실제 코드에서 따라 하면 안 된다는 것만 알아두면 된다.

---

## 9. synchronized의 한계와 다음 단계 💡

### 9.1 할 수 없는 것들

```java
synchronized (lock) {
    // ...
}
```

| 한계 | 설명 |
| --- | --- |
| ⏱ **타임아웃 불가** | "1초만 기다리고 포기"가 안 된다 |
| ✋ **취소 불가** | `BLOCKED`는 **인터럽트에 반응하지 않는다** (앞 글 참고) |
| 🔓 **범위 고정** | 블록 안에서 획득하고 블록 끝에서 해제. 유연하지 않음 |
| 📖 **읽기/쓰기 구분 없음** | 읽기만 하는 스레드끼리도 서로 막는다 |
| ⚖️ **공정성 선택 불가** | 항상 불공정 |

### 9.2 `ReentrantLock`이 해결하는 것

```java
private final ReentrantLock lock = new ReentrantLock();

// 타임아웃
if (lock.tryLock(1, TimeUnit.SECONDS)) {
    try { ... } finally { lock.unlock(); }
} else {
    log("1초 기다렸는데 못 얻음 — 포기");
}

// 인터럽트 가능
lock.lockInterruptibly();

// 공정 모드
new ReentrantLock(true);
```

| | `synchronized` | `ReentrantLock` |
| --- | --- | --- |
| 해제 | ✅ 자동 | ⚠️ `finally`에서 직접 |
| 타임아웃 | ❌ | ✅ `tryLock(time)` |
| 인터럽트 | ❌ (`BLOCKED`) | ✅ `lockInterruptibly()` |
| 공정성 | 불공정 고정 | ✅ 선택 가능 |
| 읽기/쓰기 분리 | ❌ | ✅ `ReadWriteLock` |
| 코드 간결성 | ✅ | 장황함 |

> 💡 **성능은 요즘 거의 차이가 없다.** 예전에는 `ReentrantLock`이 빨랐지만, JVM의 락 최적화가 발전하면서 비슷해졌다. **기능이 더 필요할 때만 `ReentrantLock`을 쓰고, 기본은 `synchronized`**가 권장된다.

### 9.3 💡 가상 스레드와 synchronized (JDK 버전 주의)

| JDK | 상황 |
| --- | --- |
| 21~23 | 가상 스레드가 `synchronized` 안에서 블로킹되면 **캐리어 스레드를 고정(pinning)**시켜 성능이 급락 → `ReentrantLock` 권장 |
| 24+ | JEP 491로 해결됨 → `synchronized`도 안전 |

가상 스레드를 쓸 계획이라면 JDK 버전을 확인하는 게 좋다.

---

## 10. 원문 점검 결과 요약

### 🔴 정정

| # | 원문 | 정정 |
| --- | --- | --- |
| 1 | `balance =- amount` | **`balance -= amount`**. `=-`는 컴파일되는 다른 코드(`= -amount`)라 더 위험 |
| 2 | "같은 balance가 둘 다 **10000원**으로 일고" | **1000원으로 읽고** |
| 3 | "멀티 스레드를 사용하 때는 / 동시서 문제" | 사용할 때는 / 동시성 문제 |
| 4 | "락을 획득하는지는 자바 표준에 정의되어있지 않음" | "락을 획득하는 **순서는** 정의되어 있지 않음" |
| 5 | V3에서 `getBalance()`에 `synchronized` 추가 (설명 없음) | **V2 시점에도 필요**. 왜 필요한지 짚어야 함 |
| 6 | "V3로 임계 영역을 작게 만들었다" | 실제로 줄어든 건 **로그 두 줄**. `sleep(1000)`이 그대로 락 안에 있음 |

### 🟡 보강

| # | 항목 | 보강 내용 |
| --- | --- | --- |
| 1 | 두 가지 시나리오 설명 | 실제로 터진 건 **check-then-act**. 결과가 200이 아닌 **-600**인 게 증거 |
| 2 | "volatile 때문 아님" | 맞음. **최신 값을 제대로 읽고도** 터졌다는 게 핵심 |
| 3 | `sleep(1000)`의 역할 | 문제를 **100% 재현시키는 장치**. 실제로는 나노초 틈이라 가끔 터짐 |
| 4 | `t1: TIMED_WAITING` | **자면서도 락을 쥐고 있다**는 결정적 장면 |
| 5 | 락 획득 순서 | 불공정 락, **기아 가능**, barging 발생 |
| 6 | "가시성도 해결됨" | **락을 잡고 접근할 때만**. `getBalance()`가 반례 |
| 7 | 블록 안 `return` | 안전하지만 **로그 동작이 V1/V2와 달라짐** |

### 💡 추가

| # | 내용 | 위치 |
| --- | --- | --- |
| 1 | check-then-act vs lost update 구분 | 2.2 |
| 2 | 경쟁 상태 / 상호 배제 / 원자성 용어 정리 | 3.3 |
| 3 | ⭐ **인스턴스 락 vs 클래스 락** — 서로 다른 락 | 5.1 |
| 4 | ⭐ **재진입(reentrant)** — 카운터 방식 | 5.2 |
| 5 | 예외가 나도 **락은 자동 해제**. 단 데이터는 안 지켜줌 | 5.3 |
| 6 | ⚠️ `synchronized(this)`는 **락이 외부에 노출**됨 | 7.1 |
| 7 | ⚠️ `String`/`Integer`를 락으로 쓰면 안 되는 이유 | 7.3 |
| 8 | ⭐ **예약 후 확정 패턴** — 락 밖으로 I/O 빼기 | 8.3 |
| 9 | 락 안에 두면 안 되는 것 체크리스트 | 8.3 |
| 10 | `synchronized`의 5가지 한계와 `ReentrantLock` | 9장 |
| 11 | 가상 스레드 pinning (JDK 21~23 주의) | 9.3 |

### 이번 글의 핵심 한 줄

> **`volatile`은 "값을 언제 보느냐"를 풀고, `synchronized`는 "누가 언제 들어오느냐"를 푼다.**
> 그리고 **락을 쥐고 있는 시간이 곧 처리량의 상한**이다.